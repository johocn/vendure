# 阶段10：履约闭环（订单状态机由包裹生命周期驱动 + 交易完成终态）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 让拆单包裹（OrderPackage）与同城配送（DeliveryOrder）的生命周期变化自动驱动订单（Order）状态在 `PartiallyShipped → Shipped → PartiallyDelivered → Delivered → Completed` 间流转；新增 `Completed`（交易完成）终态，支持**手动确认收货**（admin `completeOrder` / C端 `confirmOrderReceipt`）与**自动交易完成**（Delivered 后 N 天，`order-complete-auto` 定时扫描）。self 包 fulfillment 状态镜像以保 core 一致性。

**Architecture:** 方案 A（包裹聚合驱动 + 自定义订单进程）。`OrderPackageService.transition` 是逐包状态唯一收敛点（发货挂钩/配送事件/markPackageDelivered 均经它），在其中落库成功后调用 `reconcileOrderState` 按包裹聚合回写订单状态；自定义 `OrderProcess` 新增 `Completed` 并禁用 `checkFulfillmentStates`（city 包无 fulfillment 不被拦截）；self 包 shipped/delivered 时镜像 fulfillment 状态（失败仅告警）。

**Tech Stack:** TypeScript + Vendure core `OrderProcess`/`OrderStates` 接口增强 + `configureDefaultOrderProcess`（已从 `@vendure/core` 导出）+ `ScheduledTask`（复用 OrderTimeoutPlugin 模式）+ 既有 `.mjs` e2e 脚本模式。

---

## 文件清单

| 动作 | 文件路径 | 责任 |
|---|---|---|
| **创建** | `packages/logistics-plugin/src/order-completion.process.ts` | 自定义订单进程：`Completed` 状态增强 + `Delivered→Completed` 转移 + `__logisticsClosure` 幂等标记 |
| **修改** | `packages/logistics-plugin/src/order-package.service.ts` | `transition()` 落库成功后触发 `reconcileOrderState` + self 包 fulfillment 镜像；新增 `reconcileOrderState` / `markDeliveredAt` / `transitionFulfillmentForPackage` |
| **修改** | `packages/logistics-plugin/src/catalog-custom-fields.ts` | `Order.fulfillmentDeliveredAt`（datetime，nullable，public） |
| **修改** | `packages/logistics-plugin/src/channel-custom-fields.ts` | `Channel.orderCompleteDays`（int，可空，覆盖自动完成天数） |
| **修改** | `packages/logistics-plugin/src/types.ts` | `LogisticsPluginOptions.defaultCompleteDays`（默认兜底） |
| **创建** | `packages/logistics-plugin/src/order-complete-auto.service.ts` | `scanForAutoComplete(ctx, now)` 扫描 `Delivered 且超期` → `Completed`；`runAutoCompleteScan` 供 mutation 触发 |
| **修改** | `packages/logistics-plugin/src/plugin.ts` | 注册 `orderCompletionProcess` + `order-complete-auto` ScheduledTask（幂等）；`onApplicationBootstrap` 注入 order-complete 服务 |
| **修改** | `packages/logistics-plugin/src/order-package-shop.resolver.ts` | C端 `confirmOrderReceipt` mutation（归属校验，幂等） |
| **修改** | `packages/logistics-plugin/src/split-admin.resolver.ts` | admin `completeOrder` / `runAutoCompleteScan` mutation |
| **修改** | `packages/logistics-plugin/src/plugin.ts`（adminSchema/shopSchema） | 新增 mutation 声明 |
| **修改** | `packages/dev-server/dev-config.ts` | `LogisticsPlugin.init({ defaultCompleteDays: 3 })` |
| **创建** | `tools/e2e-phase10-fulfillment-closure.mjs` | 阶段10 e2e：t1-t9 订单状态聚合/镜像/手动/自动/回归 |
| **修改** | 仓库根 `.gitignore`（如已有 dist 规则，无需动） | 无需 |

---

## Task 1：自定义订单进程（`Completed` + 禁用 `checkFulfillmentStates`）

**Files:**
- Create: `packages/logistics-plugin/src/order-completion.process.ts`

- [ ] **Step 1：实现 order-completion.process.ts**

`packages/logistics-plugin/src/order-completion.process.ts`：

```typescript
import { OrderProcess } from '@vendure/core';

declare module '@vendure/core' {
    interface OrderStates {
        Completed: never;
    }
}

/**
 * 履约闭环：Delivered → Completed（交易完成终态）。
 * 通过 OrderStates 接口增强把 Completed 并入 OrderState 联合类型；
 * mergeTransitionDefinitions 会把 Delivered: {to:['Cancelled','Completed']} 与默认进程的
 * Delivered: {to:['Cancelled']} 做并集，其余状态转移保持默认进程原样。
 */
export const orderCompletionProcess: OrderProcess<any> = {
    transitions: {
        Delivered: { to: ['Cancelled', 'Completed'] },
    },
};
```

- [ ] **Step 2：tsc 类型检查（禁 pnpm build/install）**

在仓库根目录：

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
```

Expected: 无错误（`OrderStates` 接口增强在 `@vendure/core` 已声明，`OrderState` 联合类型自动含 `Completed`）。

- [ ] **Step 3：Commit**

```bash
git add packages/logistics-plugin/src/order-completion.process.ts
git commit -m "feat(logistics): add order completion process (Completed state)"
```

---

## Task 2：订单聚合回写 + Fulfillment 镜像（order-package.service.ts）

**Files:**
- Modify: `packages/logistics-plugin/src/order-package.service.ts`

- [ ] **Step 1：在 transition() 落库成功后触发聚合与镜像**

在 [order-package.service.ts#L144-L166](file:///d:/zhao/vendure/packages/logistics-plugin/src/order-package.service.ts#L144-L166) 的 `transition()` 中，`await repo.save(pkg)` 之后（`return true` 之前）插入：

```typescript
// 履约闭环：包裹状态变化后聚合驱动订单状态 + 镜像 fulfillment（失败仅告警，不阻断）
if (toStatus === 'shipped' || toStatus === 'delivered') {
    await this.mirrorFulfillment(ctx, pkg, toStatus);
}
try {
    await this.reconcileOrderState(ctx, orderId);
} catch (e: any) {
    Logger.warn(`订单状态聚合失败 order#${orderId}: ${e?.message ?? e}`, loggerCtx);
}
```

- [ ] **Step 2：实现 reconcileOrderState（包裹聚合 → 订单状态）**

在 `transition()` 之后新增方法：

```typescript
/**
 * 履约闭环核心：按包裹生命周期聚合推导订单目标状态并推进。
 * 幂等（订单已到目标状态跳过）、非法流转经 getNextOrderStates 二次过滤仅告警。
 */
async reconcileOrderState(ctx: RequestContext, orderId: ID): Promise<void> {
    if (!this.orderService) {
        Logger.warn('OrderService 未初始化，跳过订单状态聚合', loggerCtx);
        return;
    }
    const packages = await this.findByOrder(ctx, orderId);
    const hasPending = packages.some(p => p.status === 'pending');
    const shippedCount = packages.filter(p => p.status === 'shipped').length;
    const deliveredCount = packages.filter(p => p.status === 'delivered').length;

    let target: string | null = null;
    if (deliveredCount > 0 && packages.every(p => p.status === 'delivered')) {
        target = 'Delivered'; // 全部送达
    } else if (deliveredCount > 0) {
        target = 'PartiallyDelivered'; // 部分送达
    } else if (!hasPending && shippedCount > 0 && packages.every(p => p.status !== 'pending')) {
        target = 'Shipped'; // 全部发货（含 cancelled）
    } else if (shippedCount > 0) {
        target = 'PartiallyShipped'; // 部分发货
    }
    // 全部 cancelled：不推进（订单级取消是独立语义，已发货/送达包取消走售后）
    if (!target) return;

    const order = await this.orderService.findOne(ctx, orderId);
    if (!order || order.state === target) return;
    const next = this.orderService.getNextOrderStates(order);
    if (!next.includes(target as any)) {
        Logger.warn(`订单聚合目标 ${target} 不在可达状态 [${next.join(',')}]（order#${orderId} 当前 ${order.state}），跳过`, loggerCtx);
        return;
    }
    const result = await this.orderService.transitionToState(ctx, orderId, target as any);
    if (isGraphQlErrorResult(result)) {
        Logger.warn(`订单状态流转 ${order.state}->${target} 失败: ${JSON.stringify(result)}`, loggerCtx);
        return;
    }
    if (target === 'Delivered') {
        await this.markDeliveredAt(ctx, orderId); // 首次送达写 fulfillmentDeliveredAt（自动完成扫描基准）
    }
    Logger.info(`订单聚合回写 order#${orderId} -> ${target}`, loggerCtx);
}
```

- [ ] **Step 3：实现 markDeliveredAt（写 Order.fulfillmentDeliveredAt 首次）**

```typescript
/** 订单首次进入 Delivered 时写 fulfillmentDeliveredAt（自动交易完成的扫描基准；已写过则跳过） */
async markDeliveredAt(ctx: RequestContext, orderId: ID): Promise<void> {
    if (!this.orderService) return;
    const order = await this.orderService.findOne(ctx, orderId);
    if (!order || (order.customFields as any)?.fulfillmentDeliveredAt) return;
    (order.customFields as any).fulfillmentDeliveredAt = new Date();
    await this.connection.getRepository(ctx, Order).save(order, { reload: false });
}
```

- [ ] **Step 4：实现 mirrorFulfillment（self 包 fulfillment 状态镜像）**

```typescript
/** self 包 fulfillment 镜像：包裹 shipped/delivered 时同步 fulfillment 状态（core 一致性，失败仅告警） */
async mirrorFulfillment(ctx: RequestContext, pkg: OrderPackage, toStatus: 'shipped' | 'delivered'): Promise<void> {
    if (pkg.fulfillmentId == null || !this.orderService) return;
    const target = toStatus === 'shipped' ? 'Shipped' : 'Delivered';
    try {
        const res = await this.orderService.transitionFulfillmentToState(ctx, pkg.fulfillmentId, target as any);
        if (isGraphQlErrorResult(res)) {
            Logger.warn(`fulfillment#${pkg.fulfillmentId} 镜像 ${target} 失败: ${JSON.stringify(res)}`, loggerCtx);
        }
    } catch (e: any) {
        Logger.warn(`fulfillment#${pkg.fulfillmentId} 镜像 ${target} 异常: ${e?.message ?? e}`, loggerCtx);
    }
}
```

> 说明：`isGraphQlErrorResult` 需从 `@vendure/core` 导入（检查现有导入列表，若无则追加）。

- [ ] **Step 5：tsc 类型检查**

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
```

Expected: 无错误（`OrderPackageStatus` 联合类型含 `'shipped'|'delivered'`；`Order` 实体已导入）。

- [ ] **Step 6：Commit**

```bash
git add packages/logistics-plugin/src/order-package.service.ts
git commit -m "feat(logistics): package-driven order state reconcile + fulfillment mirror"
```

---

## Task 3：自定义字段 + 插件选项

**Files:**
- Modify: `packages/logistics-plugin/src/catalog-custom-fields.ts`
- Modify: `packages/logistics-plugin/src/channel-custom-fields.ts`
- Modify: `packages/logistics-plugin/src/types.ts`

- [ ] **Step 1：Order.fulfillmentDeliveredAt 自定义字段**

在 [catalog-custom-fields.ts#L100](file:///d:/zhao/vendure/packages/logistics-plugin/src/catalog-custom-fields.ts#L100) 的 `Order: [...]` 末尾追加：

```typescript
{
    name: 'fulfillmentDeliveredAt',
    type: 'datetime',
    nullable: true,
    public: true,
    label: [{ languageCode: LanguageCode.zh_Hans, value: '履约完成时间（订单首次送达）' }],
},
```

- [ ] **Step 2：Channel.orderCompleteDays 自定义字段**

在 [channel-custom-fields.ts#L37](file:///d:/zhao/vendure/packages/logistics-plugin/src/channel-custom-fields.ts#L37) 的 `Channel: [...]` 末尾追加：

```typescript
{
    name: 'orderCompleteDays',
    type: 'int',
    nullable: true,
    label: [{ languageCode: LanguageCode.zh_Hans, value: '自动交易完成天数（送达后 N 天，覆盖默认值）' }],
},
```

- [ ] **Step 3：插件选项 defaultCompleteDays**

在 [types.ts](file:///d:/zhao/vendure/packages/logistics-plugin/src/types.ts) 追加：

```typescript
export interface LogisticsPluginOptions {
    defaultShippingStrategy?: 'priority' | 'nearest' | 'stock-first';
    trackingProvider?: LogisticsTrackingProvider;
    /** 自动交易完成：订单 Delivered 后 N 天未确认收货自动转 Completed（Channel.orderCompleteDays 可覆盖） */
    defaultCompleteDays?: number;
}
```

- [ ] **Step 4：tsc 类型检查 + Commit**

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
git add packages/logistics-plugin/src/catalog-custom-fields.ts packages/logistics-plugin/src/channel-custom-fields.ts packages/logistics-plugin/src/types.ts
git commit -m "feat(logistics): add fulfillmentDeliveredAt + orderCompleteDays custom fields"
```

---

## Task 4：自动交易完成服务（order-complete-auto.service.ts）

**Files:**
- Create: `packages/logistics-plugin/src/order-complete-auto.service.ts`

- [ ] **Step 1：实现扫描服务**

`packages/logistics-plugin/src/order-complete-auto.service.ts`：

```typescript
import { Injectable } from '@nestjs/common';
import { ID, Injector, Logger, Order, OrderService, RequestContext, TransactionalConnection } from '@vendure/core';
import { In, LessThanOrEqual } from 'typeorm';
import { loggerCtx } from './constants';

/**
 * 自动交易完成：扫描 state='Delivered' 且 fulfillmentDeliveredAt <= now - completeDays 的订单
 * 推进到 Completed。手动（runAutoCompleteScan）/定时（order-complete-auto）共用。
 */
@Injectable()
export class OrderCompleteAutoService {
    private injector: Injector | null = null;
    private orderService: OrderService | null = null;

    init(injector: Injector): void {
        this.injector = injector;
        try {
            this.orderService = injector.get(OrderService);
        } catch (e) {
            Logger.warn('OrderCompleteAutoService 无法获取 OrderService', loggerCtx);
            this.orderService = null;
        }
    }

    /** 扫描并自动完成；返回本次完成订单数 */
    async runAutoCompleteScan(ctx: RequestContext, now = new Date()): Promise<number> {
        if (!this.orderService || !this.injector) return 0;
        const connection = this.injector.get(TransactionalConnection);
        const options = this.injector.get('LOGISTICS_PLUGIN_OPTIONS') as { defaultCompleteDays?: number };
        const completeDays = (ctx.channel as any)?.customFields?.orderCompleteDays ?? options?.defaultCompleteDays ?? 3;
        const deadline = new Date(now.getTime() - completeDays * 24 * 60 * 60 * 1000);

        // 注意：fulfillmentDeliveredAt 是自定义字段，typeorm 查询按原始列名 customFields 前缀
        const repo = connection.getRepository(ctx, Order);
        const candidates = await repo
            .createQueryBuilder('order')
            .where('order.state = :state', { state: 'Delivered' })
            .andWhere('order.active = :active', { active: true })
            .getMany();

        let done = 0;
        for (const order of candidates) {
            const deliveredAt = (order.customFields as any)?.fulfillmentDeliveredAt;
            if (!deliveredAt) continue;
            if (new Date(deliveredAt).getTime() > deadline.getTime()) continue;
            const result = await this.orderService.transitionToState(ctx, order.id, 'Completed' as any);
            if ((result as any)?.state === 'Completed' || !('errorCode' in (result as any))) {
                done++;
                Logger.info(`自动交易完成 order#${order.code ?? order.id}`, loggerCtx);
            }
        }
        return done;
    }
}
```

- [ ] **Step 2：tsc 类型检查**

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
```

Expected: 无错误（若 `Order.customFields` 类型不识别 `fulfillmentDeliveredAt`，用 `(order.customFields as any)` 规避，已按此写法）。

- [ ] **Step 3：Commit**

```bash
git add packages/logistics-plugin/src/order-complete-auto.service.ts
git commit -m "feat(logistics): add auto order complete scan service"
```

---

## Task 5：插件注册（order process + ScheduledTask + 服务注入 + schema）

**Files:**
- Modify: `packages/logistics-plugin/src/plugin.ts`

- [ ] **Step 1：imports 与 schema mutation 声明**

顶部追加 import：

```typescript
import { configureDefaultOrderProcess } from '@vendure/core';
import { orderCompletionProcess } from './order-completion.process';
import { OrderCompleteAutoService } from './order-complete-auto.service';
```

`adminSchema` 的 `extend type Mutation` 追加：

```graphql
completeOrder(orderId: ID!): Boolean!
runAutoCompleteScan: Int!
```

`shopSchema` 的 `extend type Mutation`（无则新增）追加：

```graphql
confirmOrderReceipt(orderId: ID!): Boolean!
```

> 注意：当前 shopSchema 只有 `extend type Query`，需新增一个 `extend type Mutation { confirmOrderReceipt(orderId: ID!): Boolean! }` 块。

- [ ] **Step 2：configuration 钩子注册订单进程 + 定时任务**

在 [plugin.ts#L194-L210](file:///d:/zhao/vendure/packages/logistics-plugin/src/plugin.ts#L194-L210) `configuration: (config) => { ... }` 中追加：

```typescript
// 履约闭环：包裹聚合驱动订单状态机（禁用 checkFulfillmentStates，city 包无 fulfillment 不拦截）
if (!(config.orderOptions.process ?? []).some(p => (p as any).__logisticsClosure)) {
    config.orderOptions.process = [
        configureDefaultOrderProcess({ checkFulfillmentStates: false }),
        { ...orderCompletionProcess, __logisticsClosure: true } as any,
    ];
}
// 自动交易完成定时任务（幂等注册，复用 OrderTimeoutPlugin 的补偿扫描模式）
if (!config.schedulerOptions.tasks.some(t => t.id === 'order-complete-auto')) {
    config.schedulerOptions.tasks.push(
        new ScheduledTask({
            id: 'order-complete-auto',
            description: 'Scan Delivered orders past completion deadline and mark Completed',
            schedule: cron => cron.every(5).minutes(),
            async execute({ injector }) {
                const service = injector.get(OrderCompleteAutoService);
                await service.runAutoCompleteScan(
                    // 用默认渠道 ctx；internal 扫描无需用户上下文
                    await injector.get(ChannelService).getDefaultChannel(),
                );
            },
        }),
    );
}
```

> 说明：`ScheduledTask` 的 `execute({ injector })` 回调签名需与核心 `ScheduledTask` 类型一致（见 OrderTimeoutPlugin 用法，其直接 `injector.get(OrderTimeoutJob)`）。`runAutoCompleteScan` 第一个参数接受 `RequestContext`；此处若类型不接受直接传 Channel，则按 OrderTimeoutPlugin 的 Job 模式改造——为稳妥，**改用独立 Job 服务 + ScheduledTask 模式**（见 Step 3 修正）。

- [ ] **Step 3：（修正）按 OrderTimeoutPlugin 的 Job 模式落地**

为规避 ScheduledTask execute 回调 ctx 类型细节，**新增方法**：

```typescript
async runAutoCompleteJob(): Promise<number> {
    // 内部任务：默认渠道 + 无用户上下文
    const channel = await this.injector.get(ChannelService).getDefaultChannel();
    const ctx = new RequestContext({
        apiType: 'admin',
        channel,
        isAuthorized: true,
        authorizedAsOwnerOnly: false,
    });
    return this.orderCompleteAuto.runAutoCompleteScan(ctx);
}
```

并在 plugin class 注入 `OrderCompleteAutoService` 与 `ChannelService`、`RequestContext`，`onApplicationBootstrap` 中 `this.orderCompleteAuto.init(this.injector)`；ScheduledTask execute 改为：

```typescript
async execute({ injector }) {
    const plugin = injector.get(LogisticsPlugin);
    await plugin.runAutoCompleteJob();
},
```

> 若 `injector.get(LogisticsPlugin)` 不可用（插件类不作为 provider 暴露），则把 Job 逻辑放进 OrderCompleteAutoService 自身一个无参方法，ScheduledTask execute 直接 `injector.get(OrderCompleteAutoService)` 后调用——**以 tsc 通过为准，选择最简单可达路径**。

- [ ] **Step 4：providers 注册 OrderCompleteAutoService**

在 [plugin.ts#L177-L185](file:///d:/zhao/vendure/packages/logistics-plugin/src/plugin.ts#L177-L185) `providers` 数组追加：

```typescript
OrderCompleteAutoService,
```

- [ ] **Step 5：onApplicationBootstrap 注入**

在 [plugin.ts#L232-L237](file:///d:/zhao/vendure/packages/logistics-plugin/src/plugin.ts#L232-L237) 追加：

```typescript
this.orderCompleteAuto.init(this.injector);
```

- [ ] **Step 6：tsc 类型检查**

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
```

Expected: 无错误。若 `ScheduledTask` execute 回调注入方式与 OrderTimeout 不一致，按 Step 3 说明修正。

- [ ] **Step 7：Commit**

```bash
git add packages/logistics-plugin/src/plugin.ts
git commit -m "feat(logistics): register completion process + auto-complete scheduled task"
```

---

## Task 6：手动确认收货 mutation（admin + C端）

**Files:**
- Modify: `packages/logistics-plugin/src/split-admin.resolver.ts`
- Modify: `packages/logistics-plugin/src/order-package-shop.resolver.ts`

- [ ] **Step 1：admin completeOrder + runAutoCompleteScan**

在 [split-admin.resolver.ts](file:///d:/zhao/vendure/packages/logistics-plugin/src/split-admin.resolver.ts) 追加：

```typescript
@Mutation()
@Allow(Permission.UpdateOrder)
async completeOrder(@Ctx() ctx: RequestContext, @Args('orderId') orderId: string): Promise<boolean> {
    const order = await this.orderService.findOne(ctx, orderId);
    if (!order) return false;
    if (order.state === 'Completed') return true; // 幂等
    const result = await this.orderService.transitionToState(ctx, orderId, 'Completed' as any);
    return !isGraphQlErrorResult(result);
}

@Mutation()
@Allow(Permission.UpdateOrder)
async runAutoCompleteScan(@Ctx() ctx: RequestContext): Promise<number> {
    return this.orderCompleteAuto.runAutoCompleteScan(ctx);
}
```

> 需在 constructor 注入 `OrderService` 与 `OrderCompleteAutoService`（`isGraphQlErrorResult` 从 `@vendure/core` 导入）。

- [ ] **Step 2：C端 confirmOrderReceipt（归属校验 + 幂等）**

在 [order-package-shop.resolver.ts](file:///d:/zhao/vendure/packages/logistics-plugin/src/order-package-shop.resolver.ts) 追加：

```typescript
@Mutation()
@Allow(Permission.Authenticated)
async confirmOrderReceipt(@Ctx() ctx: RequestContext, @Args('orderId') orderId: string): Promise<boolean> {
    return this.orderPackageService.confirmOrderReceipt(ctx, orderId as any);
}
```

> resolver 需注入 `OrderPackageService`（已注入）。`confirmOrderReceipt` 归属校验逻辑放在 OrderPackageService（复用 `getMyOrderPackages` 的 `customer.user.id` 校验模式）。

- [ ] **Step 3：OrderPackageService.confirmOrderReceipt**

在 [order-package.service.ts](file:///d:/zhao/vendure/packages/logistics-plugin/src/order-package.service.ts) 追加方法：

```typescript
/** C端确认收货：归属校验（customer.user.id === activeUserId）+ Delivered→Completed（幂等） */
async confirmOrderReceipt(ctx: RequestContext, orderId: ID): Promise<boolean> {
    if (!this.orderService) throw new Error('OrderService 未初始化');
    if (!ctx.activeUserId) throw new UnauthorizedError();
    const order = await this.orderService.findOne(ctx, orderId, ['customer', 'customer.user']);
    if (!order) throw new EntityNotFoundError('Order', orderId);
    const customerUserId = (order.customer as any)?.user?.id;
    if (!order.customer || customerUserId == null || String(customerUserId) !== String(ctx.activeUserId)) {
        throw new ForbiddenError();
    }
    if (order.state === 'Completed') return true; // 幂等
    if (order.state !== 'Delivered') {
        Logger.warn(`确认收货要求订单在 Delivered（当前 ${order.state}），跳过`, loggerCtx);
        return false;
    }
    const result = await this.orderService.transitionToState(ctx, orderId, 'Completed' as any);
    return !isGraphQlErrorResult(result);
}
```

- [ ] **Step 4：tsc 类型检查 + Commit**

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.json --noEmit
git add packages/logistics-plugin/src/split-admin.resolver.ts packages/logistics-plugin/src/order-package-shop.resolver.ts packages/logistics-plugin/src/order-package.service.ts
git commit -m "feat(logistics): manual order completion mutations (admin + customer)"
```

---

## Task 7：dev-config 配置

**Files:**
- Modify: `packages/dev-server/dev-config.ts`

- [ ] **Step 1：LogisticsPlugin.init 配置默认完成天数**

在 [dev-config.ts#L389](file:///d:/zhao/vendure/packages/dev-server/dev-config.ts#L389) 将：

```typescript
LogisticsPlugin.init(),
```

替换为：

```typescript
LogisticsPlugin.init({ defaultCompleteDays: 3 }),
```

- [ ] **Step 2：确认 schedulerOptions 默认可用**

检查 dev-config 中 `schedulerOptions`（[dev-config.ts#L252](file:///d:/zhao/vendure/packages/dev-server/dev-config.ts#L252) 附近）已有默认插件设置；若 OrderTimeoutPlugin 的补偿扫描能跑，则 `order-complete-auto` 同样可跑（同一 DefaultSchedulerPlugin）。无需改动。

- [ ] **Step 3：Commit**

```bash
git add packages/dev-server/dev-config.ts
git commit -m "chore(dev-server): enable phase10 auto-complete default days"
```

---

## Task 8：构建插件 + 重启 dev server + 本地冒烟

**Files:**
- 无新文件（仅验证流程）

- [ ] **Step 1：构建 logistics-plugin 产物（禁 pnpm build/install）**

在仓库根目录：

```bash
node node_modules/typescript/bin/tsc -p packages/logistics-plugin/tsconfig.build.json
```

验证 lib 已含新内容：

```bash
Select-String packages/logistics-plugin/lib/src/order-completion.process.js -Pattern "Completed"
Select-String packages/logistics-plugin/lib/src/order-package.service.js -Pattern "reconcileOrderState"
```

Expected: 均命中（lib 为 dev-server 实际消费产物）。

- [ ] **Step 2：重启 dev server**

1) 确认 3000 端口进程：

```bash
Get-NetTCPConnection -LocalPort 3000 | Select-Object LocalAddress,LocalPort,OwningProcess
```

2) 结束旧进程（或原启动终端 Ctrl+C）。

3) 在 `packages/dev-server` 目录重新启动（等日志出现 server running on port 3000）：

```bash
npm run dev
```

- [ ] **Step 3：本地冒烟**

观察启动日志无 schema 错误（新增 `confirmOrderReceipt`/`completeOrder`/`runAutoCompleteScan`/`Completed` 状态类型正常生成）。

---

## Task 9：阶段10 e2e

**Files:**
- Create: `tools/e2e-phase10-fulfillment-closure.mjs`

- [ ] **Step 1：创建 e2e 脚本**

`tools/e2e-phase10-fulfillment-closure.mjs`（沿用 phase8/9 脚本结构；前置：dev server + shop-a 渠道 + 双仓 + `NF-WATER-500` 两仓库存）：

| 用例 | 场景 | 断言 |
|---|---|---|
| t1 | 下单拆两仓 → confirmSplitPlan | 订单状态仍 `PaymentSettled` |
| t2 | `batchCreateFulfillment(P1)`（self） | P1 `shipped` + fulfillment 状态 `Shipped`（镜像）；订单 `PartiallyShipped` |
| t3 | `batchCreateFulfillment(P2)` | P2 `shipped`；订单 `Shipped` |
| t4 | `markPackageDelivered(P1)` | P1 `delivered` + fulfillment `Delivered`；订单 `PartiallyDelivered` |
| t5 | `markPackageDelivered(P2)` | P2 `delivered`；订单 `Delivered` + `fulfillmentDeliveredAt` 非空 |
| t6 | admin `completeOrder` | 订单 `Completed`；再调幂等返回 true |
| t7 | city 全链路：`createDelivery(mock)` → `mockDeliveryEvent(delivered)` → C端 `confirmOrderReceipt` | 订单 `Shipped→Delivered→Completed`；无 fulfillment 也成立；confirmOrderReceipt 幂等 true |
| t8 | 自动完成：回拨 `fulfillmentDeliveredAt` 至 N 天前 → `runAutoCompleteScan` | 订单 `Completed`，返回计数 ≥1 |
| t9 | 全量回归 | 10 套全绿 |

> t7 需要 admin mutation `mockDeliveryEvent`（已有 mock provider 事件模拟能力，查 delivery-gateway admin resolver 确认 mutation 名，若无则用达达 webhook 或既有 mock 事件 mutation）。
> t8 需要 admin 直改 `Order.fulfillmentDeliveredAt`（用 admin updateOrder customFields 或 SQL；优先 admin mutation，若无则 SQL 脚本）。

- [ ] **Step 2：运行阶段10 e2e**

```bash
node tools/e2e-phase10-fulfillment-closure.mjs
```

Expected: PASS 全绿（t1-t8）。

- [ ] **Step 3：全量回归（既有 9 套保持全绿）**

```bash
node tools/e2e-phase2.mjs
node tools/e2e-phase3-split.mjs
node tools/e2e-phase3-matrix.mjs
node tools/e2e-phase3-city.mjs
node tools/e2e-phase4-after-sales-restock.mjs
node tools/e2e-phase5-order-package.mjs
node tools/e2e-phase6-order-package-state.mjs
node tools/e2e-phase7-shop-order-package.mjs
node tools/e2e-phase8-per-package-fulfillment.mjs
node tools/e2e-phase9-dada-webhook.mjs
```

Expected: 9 套全 PASS（注意：若既有 e2e 断言订单停在 PaymentSettled 且被新聚合逻辑推进，需核对 phase6 的订单状态断言——阶段6 边界是"不回写订单"；本阶段会改变该行为，**如 phase6 有相应断言需同步更新**）。

- [ ] **Step 4：Commit**

```bash
git add tools/e2e-phase10-fulfillment-closure.mjs
git commit -m "test(e2e): add phase10 fulfillment closure tests"
```

---

## 验收清单（对应设计成功标准）

1. [ ] 拆单订单随包裹状态聚合自动推进：`PartiallyShipped → Shipped → PartiallyDelivered → Delivered`。
2. [ ] city 包（无 fulfillment）同样驱动订单状态，不被 `checkFulfillmentStates` 拦截。
3. [ ] self 包 fulfillment 状态镜像（shipped/delivered），后台与包裹一致。
4. [ ] 手动确认收货（admin `completeOrder` / C端 `confirmOrderReceipt`）使订单 → `Completed`，幂等。
5. [ ] 自动交易完成：Delivered 超过 N 天（可 Channel 覆盖）→ `Completed`，`runAutoCompleteScan` 可手动触发验证。
6. [ ] `Completed` 不可回退；非法流转/错误仅告警不阻断主链路。
7. [ ] 全量回归 9+1 套 e2e 全绿。
