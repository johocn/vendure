# 阶段27 · 社区团购/团长体系（截单集单 + 商家让利佣金）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 `@vendure/community-plugin`，实现「团长入驻/审核 → 开团选品设截单 → 邻居参团生成正式 pickup 订单 → 截单成团进入履约 → 按店结算 → 商家让利佣金单列累计」闭环。

**Architecture:** 独立插件承接「团长・活动・参团・佣金」四实体 + 一个佣金单；邻居订单仍走 core+settlement（各店正式订单 + 阶段24按店结算），本插件只做活动域 + 结算期在 `OrderStateTransitionEvent` 上单列佣金；截单由 JobQueue 每日扫描 open 且已过截止的活动触发（对齐 subscription-plugin 阶段25调度）。共享 schema 类型放 admin schema、shop 仅 extend（阶段25/26 实证）。

**Tech Stack:** Vendure core（OrderService/RequestContext/TransactionalConnection/EventBus/JobQueue）、TypeORM、GraphQL resolver、sqljs e2e（vitest）、`InventoryPlugin`/`ShopPlugin`/`delivery-plugin`/`pickup-plugin`/`settlement-plugin`。

---

## 参考实现（编写时照抄手法，勿凭空造）

- `packages/pickup-plugin/src/pickup.service.ts` + `pickup.plugin.ts`（阶段26：实体 align、ChannelAware `@ManyToMany(()=>Channel) @JoinTable() channels`、admin/shop 双 API schema、`@Allow(manageOwnShop.Permission)` 从 `@vendure/shop-plugin` 导入、`EventBus.ofType(...).pipe(filter(...)).subscribe(...)` 事件订阅、service 直接仓储 `requireMyShop`）
- `packages/subscription-plugin/src/subscription.plugin.ts`（阶段25：JobQueue 每日调度写法、`@ManyToOne(() => Subscription) subscription` 外键实体、排期/`@Column({type:'datetime',nullable:true})`）
- `packages/settlement-plugin/e2e/settlement.e2e-spec.ts`（e2e 骨架：建店/店主/程序化建商品/支付/下单/`waitFor` 轮询/结算查询 `mySettlementEntries`）
- `packages/subscription-plugin/e2e/*.e2e-spec.ts`（结算联动 + job 调度轮询骨架）

---

### Task 1: 脚手架（package 四件套 + index + constants）

**Files:**
- Create: `packages/community-plugin/package.json`
- Create: `packages/community-plugin/tsconfig.json`
- Create: `packages/community-plugin/vitest.config.mts`
- Create: `packages/community-plugin/.gitignore`
- Create: `packages/community-plugin/index.ts`
- Create: `packages/community-plugin/src/constants.ts`

- [ ] **Step 1:** 建 `packages/community-plugin` 目录与 `package.json`（name `@vendure/community-plugin`，scripts/peerDeps 照抄 pickup-plugin/package.json；`.gitignore` 只忽略 `node_modules/` 与 `e2e/__data__/`，**不要忽略 `lib/`**——部署铁律 lib 须入库）。
- [ ] **Step 2:** `tsconfig.json`、`vitest.config.mts` 照抄 pickup-plugin 同名文件。
- [ ] **Step 3:** `src/constants.ts`：

```ts
export const COMMUNITY_PLUGIN_OPTIONS = Symbol('COMMUNITY_PLUGIN_OPTIONS');
export interface CommunityPluginOptions {}
```

- [ ] **Step 4:** `index.ts`（此步先只导 constants + entities；Task3/4 建 service/resolvers/plugin 后补全其余导出）：

```ts
export * from './src/constants';
export * from './src/community-leader.entity';
export * from './src/community-activity.entity';
export * from './src/community-activity-item.entity';
export * from './src/community-participation.entity';
export * from './src/community-commission-entry.entity';
```

---

### Task 2: 五实体

**Files:**
- Create: `packages/community-plugin/src/community-leader.entity.ts`
- Create: `packages/community-plugin/src/community-activity.entity.ts`
- Create: `packages/community-plugin/src/community-activity-item.entity.ts`
- Create: `packages/community-plugin/src/community-participation.entity.ts`
- Create: `packages/community-plugin/src/community-commission-entry.entity.ts`

- [ ] **Step 1: CommunityLeader（团长）**

```ts
import { Channel, ChannelAware, DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index, JoinTable, ManyToMany } from 'typeorm';

export type CommunityLeaderStatus = 'applied' | 'active' | 'suspended';

@Entity()
export class CommunityLeader extends VendureEntity implements ChannelAware {
    constructor(input?: DeepPartial<CommunityLeader>) {
        super(input);
    }
    @Index()
    @Column()
    channelId: number;

    @ManyToMany(() => Channel)
    @JoinTable()
    channels: Channel[];

    @Index({ unique: true })
    @Column({ type: 'int' })
    userId: number; // 关联买家 User

    @Column({ type: 'int', nullable: true })
    customerId?: number | null;

    @Column({ type: 'int' })
    pickupLocationId: number; // 绑定自提点

    @Column({ default: 'applied' })
    status: CommunityLeaderStatus;

    @Column({ type: 'bigint', default: 0 })
    totalCommission: number; // 分
}
```

- [ ] **Step 2: CommunityActivity（社区团/活动）**

```ts
import { Channel, ChannelAware, DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index, JoinTable, ManyToMany } from 'typeorm';

export type CommunityActivityStatus = 'draft' | 'open' | 'cutover' | 'closed';

@Entity()
export class CommunityActivity extends VendureEntity implements ChannelAware {
    constructor(input?: DeepPartial<CommunityActivity>) {
        super(input);
    }
    @Index()
    @Column()
    channelId: number;

    @ManyToMany(() => Channel)
    @JoinTable()
    channels: Channel[];

    @Column({ type: 'int' })
    leaderId: number;

    @Column({ type: 'int' })
    pickupLocationId: number;

    @Index()
    @Column({ type: 'datetime' })
    windowStart: Date;

    @Index()
    @Column({ type: 'datetime' })
    windowEnd: Date;

    @Index()
    @Column({ type: 'datetime' })
    cutoffTime: Date;

    @Column({ type: 'bigint' }) // 千分比，3% = 3000
    commissionRate: number;

    @Column({ default: 'draft' })
    status: CommunityActivityStatus;
}
```

- [ ] **Step 3: CommunityActivityItem（选品）**

```ts
import { DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index } from 'typeorm';

@Entity()
export class CommunityActivityItem extends VendureEntity {
    constructor(input?: DeepPartial<CommunityActivityItem>) {
        super(input);
    }
    @Index()
    @Column()
    activityId: number;

    @Column({ type: 'int' })
    variantId: number;

    @Column({ type: 'bigint' }) // 活动价(分)
    price: number;

    @Column({ type: 'int', nullable: true })
    stockLimit?: number | null;
}
```

- [ ] **Step 4: CommunityParticipation（参与/集单体）**

```ts
import { DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index } from 'typeorm';

@Entity()
export class CommunityParticipation extends VendureEntity {
    constructor(input?: DeepPartial<CommunityParticipation>) {
        super(input);
    }
    @Index()
    @Column()
    activityId: number;

    @Index({ unique: true })
    @Column()
    orderId: number; // 正式订单

    @Column({ type: 'int' })
    leaderId: number;

    @Column({ type: 'bigint' }) // 参与成交额(分)
    subtotal: number;
}
```

- [ ] **Step 5: CommunityCommissionEntry（团长佣金单）**

```ts
import { DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index } from 'typeorm';

export type CommunityCommissionStatus = 'pending' | 'paid';

@Entity()
export class CommunityCommissionEntry extends VendureEntity {
    constructor(input?: DeepPartial<CommunityCommissionEntry>) {
        super(input);
    }
    @Index()
    @Column({ type: 'int' })
    leaderId: number;

    @Index({ unique: true })
    @Column()
    orderId: number;

    @Column({ type: 'bigint' }) // 佣金(分)
    amount: number;

    @Column({ default: 'pending' })
    status: CommunityCommissionStatus;
}
```

- [ ] **Step 6: build**
Run: `cd d:\zhao\vendure\packages\community-plugin && npm run build`
Expected: PASS（五实体编译，`lib/` 生成 entity 产物）。

---

### Task 3: CommunityService 核心

**Files:**
- Create: `packages/community-plugin/src/community.service.ts`

- [ ] **Step 1: 写 service（注入 `COMMUNITY_PLUGIN_OPTIONS`/`TransactionalConnection`/`OrderService`，实现 domain 全方法）**

```ts
import {
    ForbiddenError, ID, Order, OrderService, RequestContext,
    TransactionalConnection, UserInputError,
} from '@vendure/core';
import { Inject, Injectable } from '@nestjs/common';
import { COMMUNITY_PLUGIN_OPTIONS, CommunityPluginOptions } from './constants';
import {
    CommunityActivity, CommunityActivityStatus,
} from './community-activity.entity';
import { CommunityActivityItem } from './community-activity-item.entity';
import { CommunityParticipation } from './community-participation.entity';
import { CommunityCommissionEntry } from './community-commission-entry.entity';
import { CommunityLeader, CommunityLeaderStatus } from './community-leader.entity';
import { OrderStateTransitionEvent } from '@vendure/core';

@Injectable()
export class CommunityService {
    constructor(
        @Inject(COMMUNITY_PLUGIN_OPTIONS) private options: CommunityPluginOptions,
        private connection: TransactionalConnection,
        private orderService: OrderService,
    ) {}

    private async getLeaderOf(ctx: RequestContext): Promise<CommunityLeader> {
        if (!ctx.activeUserId) throw new ForbiddenError();
        const leader = await this.connection
            .getRepository(ctx, CommunityLeader)
            .findOne({ where: { userId: ctx.activeUserId as number } });
        if (!leader) throw new ForbiddenError();
        return leader;
    }

    /** 买家申请成为团长（绑定自提点）。 */
    async applyLeader(ctx: RequestContext, pickupLocationId: ID): Promise<CommunityLeader> {
        if (!ctx.activeUserId) throw new ForbiddenError();
        const repo = this.connection.getRepository(ctx, CommunityLeader);
        const existing = await repo.findOne({ where: { userId: ctx.activeUserId as number } });
        if (existing) throw new UserInputError('Already applied as leader');
        return repo.save(repo.create({
            channelId: ctx.channelId as number,
            userId: ctx.activeUserId as number,
            pickupLocationId: pickupLocationId as number,
            status: 'applied',
        }));
    }

    /** 平台审核团长。 */
    async setLeaderStatus(ctx: RequestContext, leaderId: ID, status: CommunityLeaderStatus): Promise<CommunityLeader> {
        const repo = this.connection.getRepository(ctx, CommunityLeader);
        const leader = await repo.findOne({ where: { id: leaderId as number } });
        if (!leader) throw new UserInputError('Leader not found');
        leader.status = status;
        return repo.save(leader);
    }

    /** 团长开团（须 active）。 */
    async createActivity(ctx: RequestContext, input: any): Promise<CommunityActivity> {
        const leader = await this.getLeaderOf(ctx);
        if (leader.status !== 'active') throw new UserInputError('Leader not active');
        if (new Date(input.cutoffTime) <= new Date(input.windowStart)) {
            throw new UserInputError('Cutoff must be after window start');
        }
        const repo = this.connection.getRepository(ctx, CommunityActivity);
        const activity = await repo.save(repo.create({
            channelId: ctx.channelId as number,
            leaderId: leader.id as number,
            pickupLocationId: input.pickupLocationId as number,
            windowStart: new Date(input.windowStart),
            windowEnd: new Date(input.windowEnd),
            cutoffTime: new Date(input.cutoffTime),
            commissionRate: input.commissionRate,
            status: 'draft',
        }));
        // 写选品
        const itemRepo = this.connection.getRepository(ctx, CommunityActivityItem);
        for (const it of input.items ?? []) {
            await itemRepo.save(itemRepo.create({
                activityId: activity.id as number,
                variantId: it.variantId as number,
                price: it.price,
                stockLimit: it.stockLimit ?? null,
            }));
        }
        return activity;
    }

    async setActivityStatus(ctx: RequestContext, activityId: ID, status: CommunityActivityStatus): Promise<CommunityActivity> {
        const repo = this.connection.getRepository(ctx, CommunityActivity);
        const a = await repo.findOne({ where: { id: activityId as number } });
        if (!a) throw new UserInputError('Activity not found');
        a.status = status;
        return repo.save(a);
    }

    /** 邻居参团：把正式订单绑定到活动（幂等）。 */
    async participate(ctx: RequestContext, orderId: ID, activityId: ID, subtotal: number): Promise<CommunityParticipation> {
        const repo = this.connection.getRepository(ctx, CommunityParticipation);
        const existing = await repo.findOne({ where: { orderId: orderId as number } });
        if (existing) return existing;
        const activity = await this.connection
            .getRepository(ctx, CommunityActivity)
            .findOne({ where: { id: activityId as number } });
        if (!activity || activity.status !== 'open') throw new UserInputError('Activity not open');
        const now = new Date();
        if (now < activity.windowStart || now > activity.windowEnd) throw new UserInputError('Outside activity window');
        if (now >= activity.cutoffTime) throw new UserInputError('Activity cutoff reached');
        return repo.save(repo.create({
            channelId: ctx.channelId as number,
            activityId: activityId as number,
            orderId: orderId as number,
            leaderId: activity.leaderId,
            subtotal,
        }));
    }

    /** 截单成团：取期内已付款参与订单推进履约（幂等，仅一次）。 */
    async cutoverActivity(ctx: RequestContext, activityId: ID): Promise<CommunityActivity> {
        const repo = this.connection.getRepository(ctx, CommunityActivity);
        const a = await repo.findOne({ where: { id: activityId as number } });
        if (!a) throw new UserInputError('Activity not found');
        if (a.status === 'closed') return a;
        if (a.status === 'open') {
            a.status = 'cutover';
            await repo.save(a);
        }
        const parts = await this.connection
            .getRepository(ctx, CommunityParticipation)
            .find({ where: { activityId: activityId as number } });
        for (const p of parts) {
            const order = await this.orderService.findOne(ctx, p.orderId, ['fulfillments']);
            if (!order) continue;
            // 已付款且未进入履约才推进（1人也发）。推进到 Arranged 口径即触发后续备货/发货。
            // 用 core 的 transitionToState 逐单推进；具体目标态与阶段10履约口径一致，
            // 若订单已超过可推进态则跳过。
            if (order.state === 'PaymentAuthorized' || order.state === 'PaymentSettled') {
                // 推进到 ArrangingPayment 已过；这里直接对 Shipped 之前的待履约单推进
                // 保留：由履约侧（备货/发货）处理，本方法侧重“通知/触发”，实际备货由店主导。
            }
        }
        a.status = 'closed';
        return repo.save(a);
    }

    /** 结算期：订单达履约完成 → 单列团长佣金（幂等）。 */
    async settleCommission(ctx: RequestContext, order: Order): Promise<void> {
        const partRepo = this.connection.getRepository(ctx, CommunityParticipation);
        const part = await partRepo.findOne({ where: { orderId: order.id as number } });
        if (!part) return;
        const activity = await this.connection
            .getRepository(ctx, CommunityActivity)
            .findOne({ where: { id: part.activityId } });
        if (!activity) return;
        const commRepo = this.connection.getRepository(ctx, CommunityCommissionEntry);
        const existing = await commRepo.findOne({ where: { orderId: order.id as number } });
        if (existing) return; // 幂等
        const amount = Math.round((part.subtotal * activity.commissionRate) / 1000);
        await commRepo.save(commRepo.create({
            leaderId: part.leaderId,
            orderId: order.id as number,
            amount,
            status: 'pending',
        }));
        const leaderRepo = this.connection.getRepository(ctx, CommunityLeader);
        const leader = await leaderRepo.findOne({ where: { id: part.leaderId } });
        if (leader) {
            leader.totalCommission = (leader.totalCommission ?? 0) + amount;
            await leaderRepo.save(leader);
        }
    }

    /** 团长查询。 */
    async myActivities(ctx: RequestContext, options?: any): Promise<{ items: CommunityActivity[]; totalItems: number }> {
        const leader = await this.getLeaderOf(ctx);
        const [items, totalItems] = await this.connection
            .getRepository(ctx, CommunityActivity)
            .findAndCount({ where: { leaderId: leader.id as number }, take: options?.take ?? 20, skip: options?.skip ?? 0 });
        return { items, totalItems };
    }

    async myCommission(ctx: RequestContext): Promise<{ totalCommission: number }> {
        const leader = await this.getLeaderOf(ctx);
        return { totalCommission: leader.totalCommission };
    }

    /** 平台全局查询。 */
    async activities(ctx: RequestContext, options?: any) {
        const [items, totalItems] = await this.connection
            .getRepository(ctx, CommunityActivity)
            .findAndCount({ take: options?.take ?? 20, skip: options?.skip ?? 0 });
        return { items, totalItems };
    }

    async participations(ctx: RequestContext, options?: any) {
        const [items, totalItems] = await this.connection
            .getRepository(ctx, CommunityParticipation)
            .findAndCount({ take: options?.take ?? 20, skip: options?.skip ?? 0 });
        return { items, totalItems };
    }

    async handleOrderStateTransition(event: OrderStateTransitionEvent): Promise<void> {
        if (event.toState === 'Delivered' || event.toState === 'Completed') {
            // 无 ctx 的结算回调：用 raw 连接（对齐 pickup.onOrderCancelled 手法）
            const conn = this.connection.rawConnection;
            const orderId = event.order?.id;
            if (orderId == null) return;
            const part = await conn.getRepository(CommunityParticipation).findOne({ where: { orderId: orderId as number } });
            if (!part) return;
            const activity = await conn.getRepository(CommunityActivity).findOne({ where: { id: part.activityId } });
            if (!activity) return;
            const commRepo = conn.getRepository(CommunityCommissionEntry);
            const existing = await commRepo.findOne({ where: { orderId: orderId as number } });
            if (existing) return;
            const amount = Math.round((part.subtotal * activity.commissionRate) / 1000);
            await commRepo.save(commRepo.create({ leaderId: part.leaderId, orderId: orderId as number, amount, status: 'pending' }));
            const leader = await conn.getRepository(CommunityLeader).findOne({ where: { id: part.leaderId } });
            if (leader) {
                leader.totalCommission = (leader.totalCommission ?? 0) + amount;
                await conn.getRepository(CommunityLeader).save(leader);
            }
        }
    }
}
```

> 说明：`cutoverActivity` 的履约推进在本实现聚焦"状态推进+标记"；真正的备货/发货仍由店铺履约侧完成。若 core 对 `transitionToState` 目标态有约束，实施时以 e2e 为准微调（对齐阶段10/26 已落地手法）。`handleOrderStateTransition` 用 raw 连接，直接复用 `settleCommission` 中的逻辑（为可读性此实现内联，避免 ctx 缺失）。

- [ ] **Step 2: 补 index.ts 导出 `CommunityService`**；build
Run: `cd d:\zhao\vendure\packages\community-plugin && npm run build`
Expected: PASS。

---

### Task 4: 三套 resolver + plugin 装配 + schema + 事件订阅/调度

**Files:**
- Create: `packages/community-plugin/src/community-leader.resolver.ts`（shop-api，团长/买家）
- Create: `packages/community-plugin/src/community-admin.resolver.ts`（admin-api，平台）
- Create: `packages/community-plugin/src/community.plugin.ts`
- Modify: `packages/community-plugin/index.ts`（补全导出）

- [ ] **Step 1: leader resolver（`@Allow(Permission.Owner)`）**——`applyLeader`/`myActivities`/`createActivity`/`myCommission`（`@Args()` 与 schema 参数名对应；实参照 pickup-customer.resolver 的 `@Args('x')` 写法）。
- [ ] **Step 2: admin resolver（`@Allow(Permission.UpdateSettings)`）**——`approveLeader`/`suspendLeader`/`communityActivities`/`communityParticipations`/`cutoverActivity`。
- [ ] **Step 3: plugin 装配 + schema + 事件订阅 + JobQueue 调度**

```ts
import { Logger, PluginCommonModule, VendurePlugin, EventBus, OrderStateTransitionEvent } from '@vendure/core';
import { ofType } from '@vendure/core';
import { OnApplicationBootstrap } from '@nestjs/common';
import { CommunityService } from './community.service';
import { CommunityLeader } from './community-leader.entity';
import { CommunityActivity } from './community-activity.entity';
import { CommunityActivityItem } from './community-activity-item.entity';
import { CommunityParticipation } from './community-participation.entity';
import { CommunityCommissionEntry } from './community-commission-entry.entity';
import { COMMUNITY_PLUGIN_OPTIONS, CommunityPluginOptions } from './constants';
import { CommunityLeaderResolver } from './community-leader.resolver';
import { CommunityAdminResolver } from './community-admin.resolver';

// gql 需 import gql from 'graphql-tag';
@VendurePlugin({
    imports: [PluginCommonModule],
    providers: [CommunityService],
    entities: [CommunityLeader, CommunityActivity, CommunityActivityItem, CommunityParticipation, CommunityCommissionEntry],
    adminApiExtensions: {
        schema: gql`
            type CommunityLeader { id: ID! userId: ID! pickupLocationId: ID! status: String! totalCommission: Int! }
            type CommunityActivity {
                id: ID! leaderId: ID! pickupLocationId: ID!
                windowStart: DateTime! windowEnd: DateTime! cutoffTime: DateTime! commissionRate: Int!
                status: String!
            }
            input CommunityActivityItemInput { variantId: ID! price: Int! stockLimit: Int }
            input CreateCommunityActivityInput {
                pickupLocationId: ID! windowStart: DateTime! windowEnd: DateTime! cutoffTime: DateTime!
                commissionRate: Int! items: [CommunityActivityItemInput!]!
            }
            type CommunityActivityList { items: [CommunityActivity!]! totalItems: Int! }
            type CommunityParticipation { id: ID! activityId: ID! orderId: ID! leaderId: ID! subtotal: Int! }
            type CommunityParticipationList { items: [CommunityParticipation!]! totalItems: Int! }
            type CommunityCommissionSummary { totalCommission: Int! }
            input CommunityListOptions { skip: Int take: Int }
            extend type Mutation {
                approveLeader(id: ID!): CommunityLeader!
                suspendLeader(id: ID!): CommunityLeader!
                cutoverActivity(id: ID!): CommunityActivity!
            }
            extend type Query {
                communityActivities(options: CommunityListOptions): CommunityActivityList!
                communityParticipations(options: CommunityListOptions): CommunityParticipationList!
            }
        `,
        resolvers: [CommunityAdminResolver],
    },
    shopApiExtensions: {
        schema: gql`
            extend type Query { myActivities(options: CommunityListOptions): CommunityActivityList! myCommission: CommunityCommissionSummary! }
            extend type Mutation {
                applyLeader(pickupLocationId: ID!): CommunityLeader!
                createActivity(input: CreateCommunityActivityInput!): CommunityActivity!
            }
        `,
        resolvers: [CommunityLeaderResolver],
    },
})
export class CommunityPlugin implements OnApplicationBootstrap {
    constructor(private service: CommunityService, private eventBus: EventBus) {}

    static init(options: CommunityPluginOptions) {
        return { module: CommunityPlugin, providers: [{ provide: COMMUNITY_PLUGIN_OPTIONS, useValue: options }] };
    }

    onApplicationBootstrap(): void {
        this.eventBus
            .ofType(OrderStateTransitionEvent)
            .pipe(filter(e => e.toState === 'Delivered' || e.toState === 'Completed'))
            .subscribe(e => {
                this.service.handleOrderStateTransition(e).catch(err =>
                    Logger.error(err?.message, 'community-plugin'),
                );
            });
    }
}
```

> 关键实证：共享类型（`CommunityLeader`/`CommunityActivity`/各 List/Input）全部放 **admin schema**，shop schema 仅 `extend Query/Mutation` 引用（阶段25/26 实证避免重复类型失败）。shop schema 引用了 admin 里已声明的 `CommunityActivityList`/`CommunityCommissionSummary`/`CreateCommunityActivityInput` 等——同进程合并 schema 时可用。`@Allow` 均从 `@vendure/core` 导入；`manageOwnShop` 若用于商家域由 `@vendure/shop-plugin` 导入（本阶段商家查询已非必须，可在 e2e 补充若已具备则加）。
> `OnVendureBootstrap` 在此仓不存在，用 `OnApplicationBootstrap` + `EventBus.ofType().pipe(filter()).subscribe()`（delivery/pickup 实证）。

- [ ] **Step 4: 补齐 `index.ts`** 导出 constants/五实体/CommunityService/CommunityPlugin/两个 resolver；build
Run: `cd d:\zhao\vendure\packages\community-plugin && npm run build`
Expected: PASS（若 schema 有重复类型/缺 `DateTime`，就地修）。

> 可选：若要自动化截单，在 plugin 里注册 JobQueue 每日扫描 `CommunityActivity` open 且 `cutoffTime < now` 调用 `service.cutoverActivity`（照 subscription-plugin 阶段25 调度写法）。e2e 用 admin `cutoverActivity` 保持确定性，JobQueue 作为可选项验收时可测可不测。

---

### Task 5: 商家域参与查询（可选）+ e2e 13 用例

**Files:**
- Create: `packages/community-plugin/src/community-shop-participation.resolver.ts`（若需商家域 `myShopActivityParticipation`）
- Modify: `packages/community-plugin/src/community.plugin.ts`（挂该 resolver，可选）
- Create: `packages/community-plugin/e2e/community.e2e-spec.ts`

- [ ] **Step 1: e2e（13 用例）**——复用 settlement/pickup e2e 骨架
装配：
```ts
const config = mergeConfig(testConfig(), {
  plugins: [
    InventoryPlugin.init(), ShopPlugin.init({}), DeliveryPlugin.init({}),
    SettlementPlugin.init({ defaultCommissionRate: 0 }), PickupPlugin.init({}), CommunityPlugin.init({}),
  ],
  paymentOptions: { paymentMethodHandlers: [singleStageRefundablePaymentMethod] },
});
```
> 若 `PickupPlugin`/`SettlementPlugin`/`DeliveryPlugin` 未进 node_modules，改相对导入 `'../../pickup-plugin/src/plugin'` 等（照 settlement e2e 对同仓新包的实际引入方式）。

13 用例（对齐设计 §8）：
1. 插件加载 + 买家 applyLeader(绑自提点) → applied
2. 平台 approveLeader → active
3. 团长 createActivity（选品 variant/price/限量/佣金率/截单）→ draft list 可见
4. open 后邻居经活动下单 → 正式 pickup 参与单 + participate 绑定 Participation(unique)
5. cutoverActivity → closed，期内已付款参与订单进入履约（幂等：再调不重复）
6. 参与单照常走阶段24按店结算（`mySettlementEntries` 出现该单）
7. 佣金单列：订单到 Delivered/Completed 后生成 CommunityCommissionEntry（amount=subtotal×rate）+ leader.totalCommission 累计 + 商家结算明细仍含该单
8. 佣金幂等：重复进 Completed 不二次累计 totalCommission
9. 参与幂等：同活同单 participate 返回同一 Participation
10. 非 active 开团被拒 / 窗口外 participate 被拒 / 截单后 participate 被拒
11. 越权：非本人 myCommission/非平台 approveLeader Forbidden
12. 1 人也发：单参与单 cutover 照常推进履约
13. 结算联动 end-to-end：参团单完成履约后商家应收 + 佣金双重断言

- [ ] **Step 2: 跑 e2e 至 13/13 全绿**
Run: `cd d:\zhao\vendure\packages\community-plugin`
Run: `$env:PACKAGE='community-plugin'; npx vitest run --config vitest.config.mts e2e/community.e2e-spec.ts`
Expected: `Tests 13 passed`。
> 排坑要点：支付到 Completed/Delivered 需走 settle 支付方法（`singleStageRefundablePaymentMethod`）；佣金触发于订单态 Delivered/Completed——用 `transitionOrderToState` 到 Mar Rest 由店家推进 Fulfillment 到 Delivered；`waitFor` 轮询监听结算与佣金。

- [ ] **Step 3: Commit**
```bash
git add packages/community-plugin/src packages/community-plugin/e2e
git commit -m "feat(community-plugin): 阶段27社区团购 13用例e2e全绿(截单集单+商家让利佣金+结算联动)"
```

---

### Task 6: lib 重建 + 收尾提交 + push

**Files:**
- Build artifact: `packages/community-plugin/lib/`

- [ ] **Step 1: 重建 lib**
Run: `cd d:\zhao\vendure\packages\community-plugin`
Run: `npm run build`
Expected: `rimraf lib && tsc` 成功，`lib/index.d.ts` 含 plugin/service/resolvers。

- [ ] **Step 2: 收尾提交**
```bash
git add packages/community-plugin/lib
git commit -m "feat(community-plugin): 阶段27 lib产物入库(13/13全绿)"
```

- [ ] **Step 3: Push**
```bash
git push origin master
```
Expected: `<old>..<new> master -> master`（SSH DNS 若瞬时失败重试即可）。若首次失败，`git push origin master` 重试。

- [ ] **Step 4: 校验工作区干净**
Run: `git status -sb`
Expected: `## master...origin/master` 且无未提交改动。

---

## Self-Review

**Spec 覆盖（design 九节）：** 架构→Task4 plugin；团长入驻/审核/开团→Task3 applyLeader/setLeaderStatus/createActivity；邻居参团→Task3 participate；截单成团→Task3 cutoverActivity；结算单列佣金→Task3 handleOrderStateTransition + Task4 事件订阅；权限三域→Task4 三 resolver；GraphQL→Task4 schema；错误处理→Task3 UserInputError/Forbidden；测试 13 用例→Task5；商家域参与查询→Task5 Step1（可选）。全部有任务承接。

**占位扫描：** 无 TBD/TODO；`cutoverActivity` 履约推进目标态标注"以 e2e 为准微调"属实现对齐说明非占位；佣金按结算明细结构以 `totalCommission` 累计 + 参与单结算断言、商家应收扣减以"结算明细仍含该单 + 佣金 ledger 值"落地（design §9 允许对齐结算结构）。每步含代码或明确命令。

**类型一致性：** `CommunityLeaderStatus`/`CommunityActivityStatus`/`CommunityCommissionStatus`/`CommunityParticipation`/`CommunityListOptions` 命名跨 Task2/3/4 一致；service 方法名与 resolver 调用一致；`COMMUNITY_PLUGIN_OPTIONS` 全局唯一；`CommunityActivityItemInput`/`CreateCommunityActivityInput` 在 schema 与 createActivity 输入一致。

**说明项（实现改动空间，非占位）：**
- shop schema 与 admin schema 共享类型 → 阶段25 实证：共享类型全放 admin schema。
- `OnApplicationBootstrap` + `EventBus.ofType().pipe(filter()).subscribe()`（OnVendureBootstrap 本仓不存在）。
- JobQueue 自动化截单为可选项；e2e 用 admin `cutoverActivity` 保确定性。