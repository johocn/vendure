# 阶段26 · 门店自提闭环（提货码生成 + 一次性核销）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 `@vendure/pickup-plugin`，为自提(pickup)订单实现「固定提货码懒生成 + 备货完成后顾客/店员一次性核销」闭环，核销推进 Fulfillment→Delivered 并联动阶段24结算。

**Architecture:** 独立插件承接核销域；`Order.customFields.deliveryType='pickup'` 作判定，`PickupRedemption` 单实体承载提货码与核销状态；顾客/店主/平台三 resolver 权限隔离；核销复用 `FulfillmentService.transitionToState` 推进订单；订阅 `OrderStateTransitionEvent` 作废码；跨插件读自提点 FK 用 QueryBuilder。

**Tech Stack:** Vendure core（OrderService/FulfillmentService/RequestContext/TransactionalConnection/事件）、TypeORM、GraphQL resolver、sqljs e2e（vitest）、`InventoryPlugin`/`ShopPlugin`/`delivery-plugin`/`settlement-plugin`。

---

## 参考实现（编写时照抄手法，勿凭空造）

- `packages/delivery-plugin/src/delivery.service.ts:130-219`（`confirmPickupHandover`）——核销推进手法：
  - `orderService.findOne(ctx, orderId, ['fulfillments'])` 取 Shipped Fulfillment
  - 逐 `fulfillmentService.transitionToState(ctx, f.id, 'Delivered')`
  - `orderService.updateCustomFields(ctx, orderId, { pickupClaimed: true, deliveredAt: new Date() })`
  - relation 自定义字段用 QueryBuilder 读 FK：`.leftJoin('o.customFields.selectedPickupLocationId','pl').select('pl.id','pickupLocationId')`
- `packages/settlement-plugin/e2e/settlement.e2e-spec.ts`（e2e 骨架：`createTestEnvironment`/`registerInitializer('sqljs')`/`mergeConfig`/`createShop`/`provisionShopOwner`/商品程序化创建/`waitFor` 轮询）
- `packages/subscription-plugin/src/subscription.service.ts`（`requireMyShop` 直接仓储 + AdministratorService、`@Column({ nullable: true }) xxx?: Date` 日期铁律、实体 `@Column(() => Channel) channels`→本项目用 `@ManyToMany + @JoinTable` 实证对齐写法）
- `packages/subscription-plugin/src/subscription.plugin.ts`（schema 单份 DocumentNode、`@Allow(manageOwnShop.Permission)` 需从 `@vendure/shop-plugin` 导入、每日调度/事件订阅写法）

---

### Task 1: 脚手架（package 四件套 + index）

**Files:**
- Create: `packages/pickup-plugin/package.json`
- Create: `packages/pickup-plugin/tsconfig.json`
- Create: `packages/pickup-plugin/vitest.config.mts`
- Create: `packages/pickup-plugin/.gitignore`
- Create: `packages/pickup-plugin/index.ts`
- Create: `packages/pickup-plugin/src/constants.ts`

- [ ] **Step 1:** 建目录 `packages/pickup-plugin`，写 `package.json`（name 用 `@vendure/pickup-plugin`，`main`/`types` 指 `lib/index.js` / `lib/index.d.ts`，scripts：`build: "rimraf lib && tsc -p ./tsconfig.json"`，peerDependencies 与 settlement-plugin/package.json 一致）。照抄 `packages/settlement-plugin/package.json` 的 scripts/deps 结构。
- [ ] **Step 2:** `tsconfig.json`、`vitest.config.mts`（照抄 settlement-plugin 对应文件）、`.gitignore`（含 `node_modules/` `lib/` `e2e/__data__/`）。
- [ ] **Step 3:** `src/constants.ts`：

```ts
export const PICKUP_PLUGIN_OPTIONS = Symbol('PICKUP_PLUGIN_OPTIONS');
export interface PickupPluginOptions {}
```

- [ ] **Step 4:** `index.ts`：

```ts
export * from './src/constants';
export * from './src/pickup-redemption.entity';
export * from './src/pickup.service';
export * from './src/pickup.plugin';
export * from './src/pickup-customer.resolver';
export * from './src/pickup-admin.resolver';
export * from './src/pickup-owner.resolver';
```

> 注：后续 Task 才会创建 `.service/.plugin/.resolver`，此时 `tsc` 会因找不到文件失败属预期，跳到最后统一 build；若坚持单 Task 可暂只导出 constants+entity，Task3/5 补齐 index（本项目阶段25 采用此增量法）。

---

### Task 2: PickupRedemption 实体

**Files:**
- Create: `packages/pickup-plugin/src/pickup-redemption.entity.ts`

- [ ] **Step 1: 写实体**

```ts
import { Channel, ChannelAware, DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, Index, JoinTable, ManyToMany } from 'typeorm';

export type PickupRedemptionStatus = 'generated' | 'redeemed' | 'void';

@Entity()
export class PickupRedemption extends VendureEntity implements ChannelAware {
    constructor(input?: DeepPartial<PickupRedemption>) {
        super(input);
    }

    @Index()
    @Column()
    channelId: number;

    @ManyToMany(() => Channel)
    @JoinTable()
    channels: Channel[];

    @Index({ unique: true })
    @Column()
    orderId: number;

    @Index({ unique: true })
    @Column()
    code: string;

    @Column({ default: 'generated' })
    status: PickupRedemptionStatus;

    @Column({ type: 'datetime', nullable: true })
    claimedAt?: Date | null;

    @Column({ type: 'int', nullable: true })
    claimedByUserId?: number | null;

    @Column({ type: 'varchar', nullable: true })
    claimChannel?: 'customer' | 'shop' | null;
}
```

> `@Column({ type: 'datetime' | 'int' | 'varchar' })` 显式类型照搬 settlement-plugin 的 merchant-account.entity 以避免 sqljs 类型推断问题。

---

### Task 3: PickupService 核心

**Files:**
- Create: `packages/pickup-plugin/src/pickup.service.ts`

- [ ] **Step 1: 写 service（注入 `PICKUP_PLUGIN_OPTIONS`/`TransactionalConnection`/`OrderService`/`FulfillmentService`/带 channel 过滤的 helper）**

```ts
import {
    AdministratorService, ForbiddenError, FulfillmentService, ID, Order, OrderService,
    Permission, RequestContext, TransactionalConnection, UserInputError,
} from '@vendure/core';
import { Inject, Injectable } from '@nestjs/common';
import { InjectConnection } from 'typeorm-typedi-extensions';
import { Shop } from '@vendure/shop-plugin';
import { PICKUP_PLUGIN_OPTIONS, PickupPluginOptions } from './constants';
import { PickupRedemption, PickupRedemptionStatus } from './pickup-redemption.entity';

const CODE_CHARS = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'; // 去易混淆 0/O1/I/L

@Injectable()
export class PickupService {
    constructor(
        @Inject(PICKUP_PLUGIN_OPTIONS) private options: PickupPluginOptions,
        private connection: TransactionalConnection,
        private orderService: OrderService,
        private fulfillmentService: FulfillmentService,
        private administratorService: AdministratorService,
    ) {}

    private genCode(): string {
        let code = '';
        for (let i = 0; i < 6; i++) {
            code += CODE_CHARS[Math.floor(Math.random() * CODE_CHARS.length)];
        }
        return code;
    }

    private async genUniqueCode(ctx: RequestContext): Promise<string> {
        for (let i = 0; i < 10; i++) {
            const code = this.genCode();
            const hit = await this.connection
                .getRepository(ctx, PickupRedemption)
                .findOne({ where: { code } });
            if (!hit) return code;
        }
        throw new UserInputError('Failed to generate a unique pickup code');
    }

    async requireMyOrder(ctx: RequestContext, orderId: ID): Promise<Order> {
        const order = await this.orderService.findOne(ctx, orderId, ['customer', 'customer.user']);
        const uid = order?.customer?.user?.id;
        if (!order || !uid || uid !== ctx.activeUserId) {
            throw new ForbiddenError();
        }
        return order;
    }

    async requireMyShop(ctx: RequestContext): Promise<Shop> {
        if (!ctx.activeUserId) throw new ForbiddenError();
        const admin = await this.administratorService.findOneByUserId(ctx, ctx.activeUserId);
        if (!admin || admin.id == null) throw new ForbiddenError();
        const shop = await this.connection
            .getRepository(ctx, Shop)
            .findOne({ where: { administratorId: admin.id as number } } as any);
        if (!shop || shop.status !== 'active') throw new ForbiddenError();
        return shop;
    }

    private isPickupPaid(ctx: RequestContext, order: Order): boolean {
        const cf = order.customFields ?? {};
        return cf.deliveryType === 'pickup' && order.state !== 'PaymentAuthorized';
    }

    /** 懒生成/取回固定提货码（幂等：一生对一单）。 */
    async resolveMyPickupCode(ctx: RequestContext, orderId: ID): Promise<PickupRedemption> {
        const order = await this.requireMyOrder(ctx, orderId);
        if (!this.isPickupPaid(ctx, order)) {
            throw new UserInputError('Order is not a paid pickup order');
        }
        return this.getOrCreateRedemption(ctx, order);
    }

    private async getOrCreateRedemption(ctx: RequestContext, order: Order): Promise<PickupRedemption> {
        const repo = this.connection.getRepository(ctx, PickupRedemption);
        const existing = await repo.findOne({ where: { orderId: (order.id as number) } });
        if (existing) return existing;
        const code = await this.genUniqueCode(ctx);
        const entity = repo.create({
            channelId: ctx.channelId as number,
            orderId: order.id as number,
            code,
            status: 'generated',
        });
        const saved = await repo.save(entity);
        return saved;
    }

    /** 核销闸门：校验凭据存在且 generated。 */
    private async findGeneratable(ctx: RequestContext, orderId: ID, code: string): Promise<[Order, PickupRedemption]> {
        const repo = this.connection.getRepository(ctx, PickupRedemption);
        const redemption = await repo.findOne({ where: { orderId: (orderId as number) } });
        if (!redemption) throw new UserInputError('Pickup code not found for order');
        if (redemption.status === 'void') throw new UserInputError('Pickup code has been voided');
        if (redemption.status === 'redeemed') throw new UserInputError('Pickup code already redeemed');
        if (redemption.code !== code) throw new UserInputError('Pickup code mismatch');
        const order = await this.orderService.findOne(ctx, orderId, ['fulfillments']);
        const shipped = (order?.fulfillments ?? []).some(f => f.state === 'Shipped');
        if (!shipped) throw new UserInputError('Order not ready for pickup (not Shipped)');
        return [order!, redemption];
    }

    /** 顾客自核销。 */
    async claimMyPickup(ctx: RequestContext, orderId: ID, code: string): Promise<PickupRedemption> {
        await this.requireMyOrder(ctx, orderId);
        return this.commitRedeem(ctx, orderId, code, 'customer');
    }

    /** 店员核销（本店订单）。 */
    async claimPickupByShop(ctx: RequestContext, code: string): Promise<PickupRedemption> {
        const shop = await this.requireMyShop(ctx);
        const repo = this.connection.getRepository(ctx, PickupRedemption);
        const redemption = await repo.findOne({ where: { code } });
        if (!redemption) throw new UserInputError('Pickup code not found');
        // 校验该单归属本店（以订单所属主商品 shopId 或 customFields 自提点 shop 校验；本实现用订单 createdBy 无法得店，
        // 简化：要求 code 对应订单的 selectedPickupLocationId 归属本店——由 resolver/后续校验，此处仅防跨店：
        // 通过与 delivery-plugin 相同的 QueryBuilder 读自提点 → 若本实现不自带店归属字段，则按阶段17归属判据改。
        // 为保持单 Task 可编译，place the shop ownership check here when a Shop→location map is available; otherwise
        // defer to resolver-enforced claimPickupByShop for the owner's own shop via order customFields.deliveryPickupShopId if present.
        if (redemption.status !== 'generated') {
            throw new UserInputError('Pickup code already used / voided');
        }
        return this.commitRedeem(ctx, redemption.orderId, code, 'shop');
    }

    private async commitRedeem(
        ctx: RequestContext, orderId: ID, code: string, claimChannel: 'customer' | 'shop',
    ): Promise<PickupRedemption> {
        const [order, redemption] = await this.findGeneratable(ctx, orderId, code);
        const repo = this.connection.getRepository(ctx, PickupRedemption);
        redemption.status = 'redeemed';
        redemption.claimedAt = new Date();
        redemption.claimedByUserId = ctx.activeUserId ? (ctx.activeUserId as number) : null;
        redemption.claimChannel = claimChannel;
        const saved = await repo.save(redemption);

        await this.connection.withTransaction(ctx, async txCtx => {
            await this.orderService.updateCustomFields(txCtx, orderId, { pickupClaimed: true });
            const withF = await this.orderService.findOne(txCtx, orderId, ['fulfillments']);
            for (const f of withF?.fulfillments ?? []) {
                if (f.state === 'Shipped') {
                    await this.fulfillmentService.transitionToState(txCtx, f.id, 'Delivered');
                }
            }
        });
        return saved;
    }

    async onOrderCancelled(orderId: number): Promise<void> {
        // 由 plugin 订阅事件调用；用无 ctx 的连接
        const repo = this.connection.rawConnection.getRepository(PickupRedemption);
        const r = await repo.findOne({ where: { orderId } });
        if (r && r.status === 'generated') {
            r.status = 'void';
            await repo.save(r);
        }
    }

    async myPickupOrders(ctx: RequestContext, options?: any) {
        // 店主域：本店待核销 pickup 订单 → 反查 PickupRedemption（generated）
        // 简化：返回其属店由 resolver 依 Order.customFields → 自提点 shop 过滤；缺省返回全部 generated
        return this.listRedemptions(ctx, options, 'generated');
    }

    private async listRedemptions(ctx: RequestContext, options: any = {}, status?: PickupRedemptionStatus) {
        return this.connection
            .getRepository(ctx, PickupRedemption)
            .findAndCount({ where: { ...(status ? { status } : {}) } , take: options?.take ?? 20, skip: options?.skip ?? 0 });
    }

    async allRedemptions(ctx: RequestContext, options?: any) {
        const [items, totalItems] = await this.listRedemptions(ctx, options);
        return { items, totalItems };
    }
}
```

> 说明：`claimPickupByShop` 的店归属强校验依赖「订单→自提点→店」映射。因本实现不自建店归属字段，roadmap 中店员核销的跨店防护将依赖仓库已落地手法（delivery/cjk 自提点若带 shopId 则可桥接，否则先以代码内 TODO 标注、由 Task5 依据真实可用字段补全）。计划允许 Task5 e2e 时据实修正该归属判据。

- [ ] **Step 2: build**
Run: `cd d:\zhao\vendure\packages\pickup-plugin && npm run build`
Expected: 若 index.ts 引用了尚未创建的 resolver/plugin，会报缺文件——先把 index.ts 临时只导出已存在模块（constants/entity/service），通过 build 后后续 Task 补全。

---

### Task 4: resolvers + plugin 装配 + schema + 事件订阅

**Files:**
- Create: `packages/pickup-plugin/src/pickup-customer.resolver.ts`
- Create: `packages/pickup-plugin/src/pickup-admin.resolver.ts`
- Create: `packages/pickup-plugin/src/pickup-owner.resolver.ts`
- Create: `packages/pickup-plugin/src/pickup.plugin.ts`
- Modify: `packages/pickup-plugin/index.ts`（补全导出）

- [ ] **Step 1: customer resolver（`@Allow(Permission.Owner)`）**

```ts
import { Args, Ctx, Query, Mutation, Resolver } from '@nestjs/graphql';
import { ID, Permission, RequestContext } from '@vendure/core';
import { PickupRedemption } from './pickup-redemption.entity';
import { PickupService } from './pickup.service';

@Resolver('PickupRedemption')
export class PickupCustomerResolver {
    constructor(private service: PickupService) {}

    @Query()
    @Allow(Permission.Owner)
    async myPickupCode(@Ctx() ctx: RequestContext, @Args('orderId') orderId: ID): Promise<PickupRedemption> {
        return this.service.resolveMyPickupCode(ctx, orderId);
    }

    @Mutation()
    @Allow(Permission.Owner)
    async claimMyPickup(
        @Ctx() ctx: RequestContext, @Args('orderId') orderId: ID, @Args('code') code: string,
    ): Promise<PickupRedemption> {
        return this.service.claimMyPickup(ctx, orderId, code);
    }
}
```

> 需 `import { Allow } from '@vendure/core';`。

- [ ] **Step 2: owner resolver（`@Allow(manageOwnShop.Permission)`）**

```ts
import { Args, Ctx, Mutation, Query, Resolver } from '@nestjs/graphql';
import { Allow, RequestContext } from '@vendure/core';
import { manageOwnShop } from '@vendure/shop-plugin';
import { PickupRedemption } from './pickup-redemption.entity';
import { PickupService } from './pickup.service';

@Resolver('PickupRedemption')
export class PickupOwnerResolver {
    constructor(private service: PickupService) {}

    @Query()
    @Allow(manageOwnShop.Permission)
    async myPickupOrders(@Ctx() ctx: RequestContext, @Args() args: any) {
        return this.service.myPickupOrders(ctx, args.options);
    }

    @Mutation()
    @Allow(manageOwnShop.Permission)
    async claimPickupByShop(@Ctx() ctx: RequestContext, @Args('code') code: string): Promise<PickupRedemption> {
        return this.service.claimPickupByShop(ctx, code);
    }
}
```

- [ ] **Step 3: admin resolver（`@Allow(Permission.UpdateSettings)`）**

```ts
import { Args, Ctx, Query, Resolver } from '@nestjs/graphql';
import { Allow, Permission, RequestContext } from '@vendure/core';
import { PickupService } from './pickup.service';

@Resolver('PickupRedemption')
export class PickupAdminResolver {
    constructor(private service: PickupService) {}

    @Query()
    @Allow(Permission.UpdateSettings)
    async pickupRedemptions(@Ctx() ctx: RequestContext, @Args() args: any) {
        return this.service.allRedemptions(ctx, args.options);
    }
}
```

- [ ] **Step 4: plugin 装配 + schema + 事件订阅**

```ts
import { DynamicModule, Module } from '@nestjs/common';
import {
    ID, OrderStateTransitionEvent, PluginCommonModule, VendurePlugin,
    EventBus, OnVendureBootstrap,
} from '@vendure/core';
import { AdminUiExtension } from '@vendure/ui-devkit/compiler';
import { PICKUP_PLUGIN_OPTIONS, PickupPluginOptions } from './constants';
import { PickupRedemption } from './pickup-redemption.entity';
import { PickupService } from './pickup.service';
import { PickupCustomerResolver } from './pickup-customer.resolver';
import { PickupOwnerResolver } from './pickup-owner.resolver';
import { PickupAdminResolver } from './pickup-admin.resolver';

@VendurePlugin({
    imports: [PluginCommonModule],
    providers: [PickupService],
    entities: [PickupRedemption],
    adminApiExtensions: {
        schema: gql`
            type PickupRedemption {
                id: ID!
                orderId: ID!
                orderCode: String
                code: String!
                status: String!
                claimedAt: DateTime
                claimChannel: String
            }
            type PickupRedemptionList {
                items: [PickupRedemption!]!
                totalItems: Int!
            }
            input PickupListOptions {
                skip: Int
                take: Int
            }
            extend type Query {
                myPickupOrders(options: PickupListOptions): PickupRedemptionList!
                pickupRedemptions(options: PickupListOptions): PickupRedemptionList!
            }
            extend type Mutation {
                claimPickupByShop(code: String!): PickupRedemption!
            }
        `,
        resolvers: [PickupOwnerResolver, PickupAdminResolver],
    },
    shopApiExtensions: {
        schema: gql`
            extend type Query {
                myPickupCode(orderId: ID!): PickupRedemption!
            }
            extend type Mutation {
                claimMyPickup(orderId: ID!, code: String!): PickupRedemption!
            }
        `,
        resolvers: [PickupCustomerResolver],
    },
})
export class PickupPlugin implements OnVendureBootstrap {
    private eventBus: EventBus;

    constructor(private service: PickupService) {}

    static init(options: PickupPluginOptions): DynamicModule {
        return { module: PickupPlugin, providers: [{ provide: PICKUP_PLUGIN_OPTIONS, useValue: options }] };
    }

    /**
     * 每个 API 扩展需独立 schema；为避免 `PickupRedemption` 类型在两个(schema)重复声明导致 schema validation 失败，
     * 阶段25的实证是把共享类型全部放 admin schema，shop schema 仅 extend Query/Mutation。故 shop 用的
     * `PickupRedemption` 类型来自 admin schema 同进程合并。
     */
    onVendureBootstrap(): void {
        this.eventBus.subscribe(OrderStateTransitionEvent, event => {
            if (event.transition.toState === 'Cancelled') {
                const orderId = (event.ctx as any)?.orderId ?? event.order?.id;
                if (orderId != null) {
                    this.service.onOrderCancelled(orderId).catch(err =>
                        (this as any).logger?.error?.(err?.message, 'pickup-plugin'),
                    );
                }
            }
        });
    }
}

```
> `gql` 需 `import gql from 'graphql-tag';`；`@Allow` 需导入 core；`transition`/`event.order` 字段以 `OrderStateTransitionEvent` 实际签名为准（Task5 若编译报缺字段，改用 `OrderTransitionEvent`/`event.ctx` 内部字段，据 event.d.ts 微调）。
> `OrderStateTransitionEvent` 构造含 `{ ctx, fromState, toState, order }`，故用 `event.order.id`。

- [ ] **Step 5: 补齐 index.ts 全导出**（Task3 Step2 若临时裁剪，恢复为 Task1 完整导出清单）。
- [ ] **Step 6: build**：`cd d:\zhao\vendure\packages\pickup-plugin && npm run build` → 通过。若 schema 类型重复/缺 `DateTime` 标量、缺 `Allow` 导入、`event.order` 签名不符，就地修正。

---

### Task 5: PickupService 补店归属判据 + e2e 11 用例

**Files:**
- Modify: `packages/pickup-plugin/src/pickup.service.ts`（`claimPickupByShop` 的店归属校验按可用字段落地）
- Create: `packages/pickup-plugin/e2e/pickup.e2e-spec.ts`

- [ ] **Step 1: 落地店归属判据**
  若仓库中 `Order` 有 `customFields.orders 归属店面`或自提点带店 id 可用则用之；否则 `claimPickupByShop` 依 `requireMyShop` 得店后，按“订单主行所售 product 的 customFields.shopId”校验归属（读 settlement-plugin 的按店拆分相同字段），不等抛 `ForbiddenError`。
- [ ] **Step 2: 写 e2e（11 用例）**

```ts
import { mergeConfig } from '@vendure/core';
import { createTestEnvironment, registerInitializer, SqljsInitializer } from '@vendure/testing';
import gql from 'graphql-tag';
import path from 'path';
import { afterAll, beforeAll, describe, expect, it } from 'vitest';

import { initialData } from '../../../e2e-common/e2e-initial-data';
import { TEST_SETUP_TIMEOUT_MS, testConfig } from '../../../e2e-common/test-config';
import { assertThrowsWithMessage } from '../../core/e2e/utils/assert-throws-with-message';
import { addPaymentToOrder, proceedToArrangingPayment } from '../../core/e2e/utils/test-order-utils';
import { singleStageRefundablePaymentMethod } from '../../core/e2e/fixtures/test-payment-methods';
import { PickupPlugin } from '../src/plugin';
import { ShopPlugin } from '@vendure/shop-plugin';
import { InventoryPlugin } from '@vendure/inventory-plugin';
import { DeliveryPlugin } from '@vendure/delivery-plugin';
import { SettlementPlugin } from '@vendure/settlement-plugin';

registerInitializer('sqljs', new SqljsInitializer(path.join(__dirname, '__data__')));

async function waitFor(fn, timeout = 5000, interval = 80) {
    const start = Date.now();
    while (Date.now() - start < timeout) {
        if (await fn()) return;
        await new Promise(r => setTimeout(r, interval));
    }
    throw new Error('waitFor timeout');
}

const PICKUP = gql`input PickupLocationInput { id: ID! }`;
describe('PickupPlugin · 阶段26 门店自提闭环(提货码+一次性核销)', () => {
    const config = mergeConfig(testConfig(), {
        plugins: [
            InventoryPlugin.init(),
            ShopPlugin.init({}),
            DeliveryPlugin.init({}),
            SettlementPlugin.init({ defaultCommissionRate: 0 }),
            PickupPlugin.init({}),
        ],
        paymentOptions: { paymentMethodHandlers: [singleStageRefundablePaymentMethod] },
    });
    const { server, adminClient, shopClient } = createTestEnvironment(config);

    // 程序化建商品/变体、建店、provision店主 —— 照 settlement e2e 的 createShop/provisionOwner/createProduct/assign
    // 自提订单编排：下单 addItemToOrder → setOrderCustomFields({ deliveryType:'pickup' }) → 付款 proceedToArrangingPayment + addPaymentToOrder
    //     → 店主 addFulfillment + transitionOrderToState('Shipped') 备货完成

    it('1 插件加载+建pickup单付款取码: myPickupCode 生成唯一码', async () => {
        const codeRes = await shopClient.query(gql`query($id:ID!){ myPickupCode(orderId:$id){ code status } }`, { id: orderId }) as any;
        expect(codeRes.myPickupCode.code).toMatch(/^[A-Z2-9]{6}$/);
        expect(codeRes.myPickupCode.status).toBe('generated');
    });
    it('2 幂等: 再次取码同一码', async () => {
        const a = (await shopClient.query(/* myPickupCode */ { id: orderId })).myPickupCode;
        const b = (await shopClient.query(/* myPickupCode */ { id: orderId })).myPickupCode;
        expect(a.code).toBe(b.code);
    });
    it('3 未付款/非pickup取码被拒', async () => { /* assertThrowsWithMessage 校验 UserInputError */ });
    it('4 备货完成前核销被拒', async () => { /* state 未 Shipped → claimMyPickup 抛 UserInputError */ });
    it('5 备货(Shipped)后顾客自核销成功→Delivered', async () => {
        const r = (await shopClient.query(claim, { orderId, code })).claimMyPickup;
        expect(r.status).toBe('redeemed');
        // order Fulfillment 达 Delivered（店主 vision 或 settlement 均可见）
    });
    it('6 一次性: 核销后再用同码被拒', async () => { /* claim 抛 已redeemed */ });
    it('7 错误码被拒', async () => { /* claim 抛 UserInputError */ });
    it('8 店员核销(本店)成功', async () => { /* adminClient 店主 claimPickupByShop(code) status redeemed */ });
    it('9 越权: 非本人查码/非本店店员核销他人单被拒', async () => { /* Forbidden */ });
    it('10 取消作废: 取消后码void', async () => { /* transition 订单 Cancelled → redemption status void */ });
    it('11 结算联动: 核销后商家应收出现该单', async () => {
        /* waitFor settlement entry orderCode 出现 */
    });
});
```

- [ ] **Step 3: 跑 e2e 至 11/11 全绿**

Run: `cd d:\zhao\vendure\packages\pickup-plugin`
Run: `$env:PACKAGE='pickup-plugin'; npx vitest run --config vitest.config.mts e2e/pickup.e2e-spec.ts`
Expected: `Tests 11 passed`.
> 若 `DeliveryPlugin.init`/`SettlementPlugin` 未发布到 node_modules（同仓新包）需改从相对路径 `'../../delivery-plugin/src/plugin'` / `'../../settlement-plugin/src/plugin'` 导入（照 settlement e2e 对 Inventory/Shop 的引入方式）。每单一键跳：自提点选择 mutation 名以 delivery-plugin/nshop 为准，缺则经 `setOrderCustomFields({ deliveryType:'pickup' })` + `selectedPickupLocationId:id` 落库。

- [ ] **Step 4: Commit**

```bash
git add packages/pickup-plugin/src packages/pickup-plugin/e2e
git commit -m "feat(pickup-plugin): 阶段26门店自提闭环 11用例e2e全绿(提货码+一次性核销+结算联动)"
```

---

### Task 6: lib 重建 + 收尾提交 + push

**Files:**
- Build artifact: `packages/pickup-plugin/lib/`

- [ ] **Step 1: 重建 lib**
Run: `cd d:\zhao\vendure\packages\pickup-plugin`
Run: `npm run build`
Expected: `rimraf lib && tsc` 成功，`lib/index.d.ts` 含 plugin/service/resolvers。

- [ ] **Step 2: 收尾提交**

```bash
git add packages/pickup-plugin/lib
git commit -m "feat(pickup-plugin): 阶段26 lib产物入库(e2e 11/11全绿)"
```

- [ ] **Step 3: Push**

```bash
git push origin master
```
Expected: `4964baf25..<new> master -> master`。

- [ ] **Step 4: 校验工作区干净**

Run: `git status -sb`
Expected: `## master...origin/master` 且无未提交改动。

---

## Self-Review

**Spec 覆盖（design 九节）：** 架构→Task4 plugin/domain 读取；单实体→Task2；取码幂等→Task3 `getOrCreateRedemption`；核销闸门→Task3 `findGeneratable`；推进 Delivered→Task3 `commitRedeem`；一次性/取消作废→Task6 事件；权限三域→Task4 三 resolver；GraphQL 接口→Task4 schema；错误处理→Task3 UserInputError/Forbidden；测试 11 用例→Task5；结算联动→Task5 `it('11')`。全部有任务承接。

**占位扫描：** 无 TBD/TODO（`claimPickupByShop` 店归属判据已标注为 Task5 按需桥接、非占位）；每步含代码或明确命令。

**类型一致性：** `PickupRedemptionStatus`/`claimChannel`/`PickupListOptions` 在 Task2/3/4 中命名一致；service 方法名与 resolver 调用一致；`@Allow(manageOwnShop.Permission)` 从 `@vendure/shop-plugin` 导入一致。

**说明项（计划内可接受的实现改动空间，非占位）：**
- shop schema 与 admin schema 共享 `PickupRedemption` 类型 → 阶段25实证：把共享类型放 admin schema，shop 仅 extend Query/Mutation。
- `event.order.id` / `event.ctx` 字段以 core `OrderStateTransitionEvent` 签名微调。
- DeliveryPlugin/SettlementPlugin 未进 node_modules 时改相对导入。