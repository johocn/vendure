# 会员储值余额钱包（Phase 33）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `recharge-card-plugin` 从「卡密兑充」升级为完整的「会员储值余额钱包」——统一 customerId 口径、新增充值单（预留网关）、admin 余额调整、统一资金流水、余额支付/退款增强。

**Architecture:** 全部落地在现有 `@vendure/recharge-card-plugin`，复用 `CustomerBalance`/`BalanceTransaction`/`RechargeCardService`。前置修复 customerId 口径（所有余额入口统一转 `customer.id`，经 `customerService.findOneByUserId` 解析）；新增 `RechargeOrder` 实体；服务/GraphQL 双 schema（shop+admin）按阶段27 铁律各自声明共享类型。

**Tech Stack:** NestJS / TypeORM / GraphQL（shop/admin 双 API）/ vitest e2e / Vendure core。金额统一「分」（int）。幂等依赖：报价单判重、single 原子 UPDATE、`RechargeOrder.id` 判重。

**Test run (per plugin dir, deployment铁律):**
```
$env:PACKAGE='recharge-card-plugin'; npx vitest run --config vitest.config.mts e2e/recharge-card.e2e-spec.ts
```
`__data__/*.sqlite` 在增列/加表后须删除重跑。

---

### Task 1: customerId 口径统一（前置修复）

**Files:**
- Modify: `packages/recharge-card-plugin/src/recharge-card.service.ts`
- Modify: `packages/recharge-card-plugin/src/balance-payment-handler.ts`

**根因**：余额消费/充值落库用 `ctx.activeUserId`（User.id），退款用 `order.customer.id`（Customer.id），两套键混用 → 同一账户余额分裂、退款贷不到扣款行。

- [ ] **Step 1: 在 `RechargeCardService` 增加 `resolveCustomerId` 私有 helper，并改 `getBalance` 走口径统一**

```typescript
// recharge-card.service.ts —— 在 class 内、`getBalance` 前插入
private async resolveCustomerId(ctx: RequestContext, customerId?: any): Promise<number> {
    if (customerId !== undefined && customerId !== null) {
        return Number(customerId);
    }
    if (!ctx.activeUserId) {
        throw new UserInputError('Must be logged in');
    }
    const customer = await this.customerService.findOneByUserId(ctx, ctx.activeUserId);
    if (!customer) {
        throw new UserInputError('Customer not found');
    }
    return customer.id;
}
```

替换 `getBalance`：
```typescript
async getBalance(ctx: RequestContext, customerId?: any): Promise<number> {
    const cid = await this.resolveCustomerId(ctx, customerId);
    const repo = this.connection.getRepository(ctx, CustomerBalance);
    const record = await repo.findOne({
        where: { customerId: cid, channelId: ctx.channelId as any },
    });
    return record?.balance ?? 0;
}
```

- [ ] **Step 2: 改 `redeemCard` 与 `findMyCards`，用 customer.id 落库**

`redeemCard` 开头（在 `if (!ctx.activeUserId)` 守卫处）改为解析 customer：
```typescript
const cid = await this.resolveCustomerId(ctx);
```
背书处 `.set({ state:'used', redeemedByCustomerId: cid, redeemedAt: new Date() })`，入账处：
```typescript
await this.applyBalanceChange(ctx, cid, card.faceValue, BalanceTransactionType.RECHARGE, {
    rechargeCardId: card.id,
});
```
日志 `Logger.info(..., `Card ${code} redeemed by customer ${cid}, ...`)`。

`findMyCards` 改为：
```typescript
async findMyCards(ctx: RequestContext): Promise<RechargeCard[]> {
    const cid = await this.resolveCustomerId(ctx);
    const repo = this.connection.getRepository(ctx, RechargeCard);
    return repo.find({
        where: { redeemedByCustomerId: cid as any },
        order: { createdAt: 'DESC' },
    });
}
```

- [ ] **Step 3: 改 `balance-payment-handler.createPayment` 用 customer.id**

```typescript
async createPayment(ctx, order, amount, args, metadata, method) {
    try {
        if (!ctx.activeUserId) {
            return { amount, state: 'Declined' as const, errorMessage: 'Not logged in', metadata: {} };
        }
        const orderWithCustomer = await orderService.findOne(ctx, order.id);
        if (!orderWithCustomer?.customer) {
            return { amount, state: 'Declined' as const, errorMessage: 'Cannot identify customer', metadata: {} };
        }
        const remainingBalance = await rechargeService.deductBalance(
            ctx, String(orderWithCustomer.customer.id), amount, order.id,
        );
        return {
            amount,
            state: 'Settled' as const,
            transactionId: `BALANCE-${order.code}-${Date.now()}`,
            metadata: { remainingBalance },
        };
    } catch (e: any) {
        Logger.error(`Balance payment failed: ${e.message}`, loggerCtx);
        return { amount, state: 'Declined' as const, errorMessage: e.message, metadata: {} };
    }
}
```

- [ ] **Step 4: 提交**

```bash
git add packages/recharge-card-plugin/src/recharge-card.service.ts packages/recharge-card-plugin/src/balance-payment-handler.ts
git commit -m "fix(recharge-card): customerId口径统一 - 余额入口一律转 customer.id"
```

---

### Task 2: 新增 `RechargeOrder` 实体（充值单，预留网关）

**Files:**
- Create: `packages/recharge-card-plugin/src/recharge-order.entity.ts`
- Modify: `packages/recharge-card-plugin/src/plugin.ts`
- Modify: `packages/recharge-card-plugin/index.ts`

- [ ] **Step 1: 创建实体**

```typescript
// recharge-order.entity.ts
import { Channel, DeepPartial, VendureEntity } from '@vendure/core';
import { Column, Entity, ManyToOne } from 'typeorm';

export type RechargeOrderStatus = 'pending' | 'paid' | 'cancelled';

@Entity()
export class RechargeOrder extends VendureEntity {
    constructor(input?: DeepPartial<RechargeOrder>) {
        super(input);
    }

    @Column({ type: 'int' }) customerId: number;

    @Column({ type: 'int' }) amount: number;

    // 显式 varchar 防 Object 反射（阶段12/27 铁律）
    @Column({ type: 'varchar' }) status: RechargeOrderStatus;

    // 预留网关口（alipay/wechat）；现无网关时留空
    @Column({ type: 'varchar', nullable: true }) paymentMethod: string | null;

    @Column({ type: 'datetime', nullable: true }) paidAt: Date | null;

    @Column({ type: 'text', nullable: true }) remark: string | null;

    @ManyToOne(() => Channel) channel: Channel;

    @Column() channelId: number;
}
```

- [ ] **Step 2: 注册实体**

在 `plugin.ts` 顶部 `import { RechargeOrder } from './recharge-order.entity';`，并把 `RechargeOrder` 加入 `entities` 数组：
```typescript
entities: [RechargeCard, RechargeCardBatch, CustomerBalance, BalanceTransaction, RechargeOrder],
```

- [ ] **Step 3: 导出**

在 `index.ts` 追加 `export * from './src/recharge-order.entity';`

- [ ] **Step 4: 提交**

```bash
git add packages/recharge-card-plugin/src/recharge-order.entity.ts packages/recharge-card-plugin/src/plugin.ts packages/recharge-card-plugin/index.ts
git commit -m "feat(recharge-card): RechargeOrder 充值单实体（pending/paid/cancelled + 预留网关）"
```

---

### Task 3: 服务能力（充值单 / admin 调整 / 流水 / 支付增强）

**Files:**
- Modify: `packages/recharge-card-plugin/src/recharge-card.service.ts`

- [ ] **Step 1: 增加充值单 API 到 `RechargeCardService`**

在 `// ===== Card Operations =====` 段之前插入「充值单」块：

```typescript
// ===== Recharge Order Operations =====

async createRechargeOrder(ctx: RequestContext, amount: number, remark?: string): Promise<RechargeOrder> {
    const cid = await this.resolveCustomerId(ctx);
    const amt = Math.floor(amount);
    if (!Number.isFinite(amt) || amt <= 0) {
        throw new UserInputError('Invalid amount');
    }
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = new RechargeOrder({
        customerId: cid,
        amount: amt,
        status: 'pending',
        paymentMethod: null,
        paidAt: null,
        remark: remark || null,
    });
    order.channelId = ctx.channelId as number;
    order.channel = ctx.channel;
    return repo.save(order);
}

async payRechargeOrder(ctx: RequestContext, id: ID): Promise<RechargeOrder> {
    const cid = await this.resolveCustomerId(ctx);
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = await repo.findOne({ where: { id: id as any, customerId: cid, channelId: ctx.channelId as any } });
    if (!order) {
        throw new UserInputError('Recharge order not found');
    }
    if (order.status !== 'pending') {
        throw new UserInputError(`Recharge order is already ${order.status}`);
    }
    await this.connection.startTransaction(ctx);
    try {
        // 幂等：按 id 判重（防止网关回调并发重复入账）
        const claim = await repo.createQueryBuilder()
            .update(RechargeOrder)
            .set({ status: 'paid', paidAt: new Date() })
            .where('id = :id AND status = :status', { id: order.id, status: 'pending' })
            .execute();
        if (claim.affected === 0) {
            throw new UserInputError(`Recharge order is already ${order.status}`);
        }
        // 入账：money in 不设余额上限，走 RECHARGE
        await this.applyBalanceChange(ctx, cid, order.amount, BalanceTransactionType.RECHARGE, {
            remark: `Recharge order #${order.id}`,
        });
        await this.connection.commitOpenTransaction(ctx);
    } catch (e) {
        await this.connection.rollBackTransaction(ctx);
        throw e;
    }
    order.status = 'paid';
    order.paidAt = new Date();
    return order;
}

async cancelRechargeOrder(ctx: RequestContext, id: ID): Promise<RechargeOrder> {
    const cid = await this.resolveCustomerId(ctx);
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = await repo.findOne({ where: { id: id as any, customerId: cid, channelId: ctx.channelId as any } });
    if (!order) {
        throw new UserInputError('Recharge order not found');
    }
    if (order.status !== 'pending') {
        throw new UserInputError(`Recharge order is already ${order.status}`);
    }
    order.status = 'cancelled';
    return repo.save(order);
}
```

`RechargeOrder` 需要在文件顶部 import：`import { RechargeOrder } from './recharge-order.entity';`

- [ ] **Step 2: 增加 admin 手工调整 + 统一流水查询**

在 `// ===== Admin Operations =====` 段后追加：

```typescript
// ===== Balance Wallet (Phase 33) =====

async adminAdjustBalance(ctx: RequestContext, input: {
    customerId: ID;
    amount: number;
    type: BalanceTransactionType;
    remark?: string;
}): Promise<number> {
    const amt = Math.floor(input.amount);
    if (!Number.isFinite(amt) || amt === 0) {
        throw new UserInputError('Invalid amount');
    }
    const targetCustomerId = Number(input.customerId);
    await this.connection.startTransaction(ctx);
    try {
        const result = await this.applyBalanceChange(ctx, targetCustomerId, amt, input.type, {
            remark: input.remark ?? null,
        });
        await this.connection.commitOpenTransaction(ctx);
        return result;
    } catch (e) {
        await this.connection.rollBackTransaction(ctx);
        throw e;
    }
}

async myBalanceTransactions(ctx: RequestContext, options?: ListQueryOptions<BalanceTransaction>): Promise<PaginatedList<BalanceTransaction>> {
    const cid = await this.resolveCustomerId(ctx);
    return this.listQueryBuilder
        .build(BalanceTransaction, options, { ctx, channelId: ctx.channelId })
        .andWhere(`BalanceTransaction.customerId = :cid`, { cid })
        .getManyAndCount()
        .then(([items, totalItems]) => ({ items, totalItems }));
}

async customerBalances(ctx: RequestContext, options?: ListQueryOptions<CustomerBalance>): Promise<PaginatedList<CustomerBalance>> {
    return this.listQueryBuilder
        .build(CustomerBalance, options, { ctx, channelId: ctx.channelId })
        .getManyAndCount()
        .then(([items, totalItems]) => ({ items, totalItems }));
}

async customerBalanceTransactions(ctx: RequestContext, customerId: ID, options?: ListQueryOptions<BalanceTransaction>): Promise<PaginatedList<BalanceTransaction>> {
    const cid = Number(customerId);
    return this.listQueryBuilder
        .build(BalanceTransaction, options, { ctx, channelId: ctx.channelId })
        .andWhere(`BalanceTransaction.customerId = :cid`, { cid })
        .getManyAndCount()
        .then(([items, totalItems]) => ({ items, totalItems }));
}

async isRechargeOrderPaid(ctx: RequestContext, id: ID): Promise<boolean> {
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = await repo.findOne({ where: { id: id as any, channelId: ctx.channelId as any } });
    return !!order && order.status === 'paid';
}
```

- [ ] **Step 3: 支付增强辅助（防重复扣减 & 退款上限）**

追加到 class 末尾：

```typescript
// ===== Balance Payment Enhancement =====

/** 该订单是否已用余额 `balance-pay` 扣过款（Authorization 防重复扣减用） */
async isOrderBalancePaid(ctx: RequestContext, orderId: ID): Promise<boolean> {
    const repo = this.connection.getRepository(ctx, BalanceTransaction);
    const tx = await repo.findOne({
        where: { orderId: orderId as any, type: BalanceTransactionType.CONSUME },
    });
    return !!tx;
}

/** 该订单通过余额累计划扣的金额（分）；createRefund 上限依据 */
async getOrderBalanceConsumed(ctx: RequestContext, orderId: ID): Promise<number> {
    const repo = this.connection.getRepository(ctx, BalanceTransaction);
    const rows = await repo.find({
        where: { orderId: orderId as any, type: BalanceTransactionType.CONSUME },
    });
    return rows.reduce((sum, r) => sum + Math.abs(r.amount), 0);
}
```

- [ ] **Step 4: 提交**

```bash
git add packages/recharge-card-plugin/src/recharge-card.service.ts
git commit -m "feat(recharge-card): 充單/入账幂等 + admin调整 + 统一流水 + 支付辅助"
```

---

### Task 4: 余额支付/退款增强（balance-payment-handler）

**Files:**
- Modify: `packages/recharge-card-plugin/src/balance-payment-handler.ts`

- [ ] **Step 1: `createPayment` 加防重复扣减**

在 `createPayment` 的 `orderWithCustomer` 解析之后、`deductBalance` 之前插入：
```typescript
const alreadyPaid = await rechargeService.isOrderBalancePaid(ctx, order.id);
if (alreadyPaid) {
    return { amount, state: 'Declined' as const, errorMessage: 'Order already paid by balance', metadata: {} };
}
```

- [ ] **Step 2: `createRefund` 加「退款不超该单余额消费」守卫**

在 `createRefund` 解析出 `orderWithCustomer.customer` 后、`addBalance` 之前插入：
```typescript
const consumed = await rechargeService.getOrderBalanceConsumed(ctx, order.id);
if (amount > consumed) {
    return { state: 'Failed' as const, metadata: { errorMessage: 'Refund exceeds order balance payment' } };
}
```

- [ ] **Step 3: 提交**

```bash
git add packages/recharge-card-plugin/src/balance-payment-handler.ts
git commit -m "feat(recharge-card): 余额支付防重复扣减 + 退款不超该单消费"
```

---

### Task 5: shop API（充值单 + 我的流水）

**Files:**
- Modify: `packages/recharge-card-plugin/src/plugin.ts`（schema）
- Create: `packages/recharge-card-plugin/src/recharge-order.resolver.ts`
- Modify: `packages/recharge-card-plugin/src/recharge-card-shop.resolver.ts`

- [ ] **Step 1: 在 plugin.ts 的 `shopApiExtensions.schema` 扩充类型**

在现有 `extend type Mutation { redeemRechargeCard ... }` 前追加（共享类型在 shop schema 声明，阶段27 铁律）：
```graphql
type RechargeOrderItem {
    id: ID!
    customerId: Int!
    amount: Int!
    status: String!
    paymentMethod: String
    paidAt: DateTime
    remark: String
    createdAt: DateTime!
}

type BalanceTransactionItem {
    id: ID!
    customerId: Int!
    type: String!
    amount: Int!
    balanceBefore: Int!
    balanceAfter: Int!
    orderId: ID
    remark: String
    createdAt: DateTime!
}

type BalanceTransactionList implements PaginatedList {
    items: [BalanceTransactionItem!]!
    totalItems: Int!
}

extend type Query {
    myRechargeBalance: Int!
    myRechargeHistory: [RechargeCard!]!
    myRechargeOrders: [RechargeOrderItem!]!
    myBalanceTransactions(options: RechargeCardListOptions): BalanceTransactionList!
}

extend type Mutation {
    redeemRechargeCard(code: String!, pin: String): RechargeResult!
    createRechargeOrder(amount: Int!, remark: String): RechargeOrderItem!
    payRechargeOrder(id: ID!): RechargeOrderItem!
    cancelRechargeOrder(id: ID!): RechargeOrderItem!
}
```

`RechargeCardListOptions` 已在 admin schema 声明；**shop 侧需独立声明一份**（阶段27 铁律——两 schema 隔离）。在 shop schema 中加入：
```graphql
input RechargeCardListOptions {
    skip: Int
    take: Int
}
```

- [ ] **Step 2: 创建 shop 充值单 resolver**

```typescript
// recharge-order.resolver.ts
import { Args, Mutation, Query, Resolver } from '@nestjs/graphql';
import { Allow, Ctx, ListQueryOptions, PaginatedList, Permission, RequestContext } from '@vendure/core';

import { BalanceTransaction } from './balance-transaction.entity';
import { RechargeCardService } from './recharge-card.service';

@Resolver()
export class RechargeOrderResolver {
    constructor(private rechargeCardService: RechargeCardService) {}

    @Query()
    @Allow(Permission.Authenticated)
    async myRechargeOrders(@Ctx() ctx: RequestContext): Promise<any[]> {
        return this.rechargeCardService.findMyRechargeOrders(ctx);
    }

    @Query()
    @Allow(Permission.Authenticated)
    async myBalanceTransactions(
        @Ctx() ctx: RequestContext,
        @Args() options: ListQueryOptions<BalanceTransaction>,
    ): Promise<PaginatedList<BalanceTransaction>> {
        return this.rechargeCardService.myBalanceTransactions(ctx, options);
    }

    @Mutation()
    @Allow(Permission.Authenticated)
    async createRechargeOrder(
        @Ctx() ctx: RequestContext,
        @Args('amount') amount: number,
        @Args('remark', { nullable: true }) remark: string,
    ): Promise<any> {
        return this.rechargeCardService.createRechargeOrder(ctx, amount, remark);
    }

    @Mutation()
    @Allow(Permission.Authenticated)
    async payRechargeOrder(@Ctx() ctx: RequestContext, @Args('id') id: number): Promise<any> {
        return this.rechargeCardService.payRechargeOrder(ctx, id);
    }

    @Mutation()
    @Allow(Permission.Authenticated)
    async cancelRechargeOrder(@Ctx() ctx: RequestContext, @Args('id') id: number): Promise<any> {
        return this.rechargeCardService.cancelRechargeOrder(ctx, id);
    }
}
```

`findMyRechargeOrders` 需要在 service 补充（Task 3 补）：在 `// ===== Recharge Order Operations =====` 末尾追加：
```typescript
async findMyRechargeOrders(ctx: RequestContext): Promise<RechargeOrder[]> {
    const cid = await this.resolveCustomerId(ctx);
    return this.connection.getRepository(ctx, RechargeOrder).find({
        where: { customerId: cid, channelId: ctx.channelId as any },
        order: { createdAt: 'DESC' },
    });
}
```

- [ ] **Step 3: 注册 resolver 到 plugin.ts `shopApiExtensions.resolvers`**

```typescript
resolvers: [RechargeCardShopResolver, RechargeOrderResolver],
```
顶部加 `import { RechargeOrderResolver } from './recharge-order.resolver';`

- [ ] **Step 4: 更新 shop 现有 `myRechargeBalance` resolver 走 resolver 层（无需改，方法签名已在服务端统一）**

- [ ] **Step 5: 提交**

```bash
git add packages/recharge-card-plugin/src/plugin.ts packages/recharge-card-plugin/src/recharge-order.resolver.ts packages/recharge-card-plugin/src/recharge-card.service.ts packages/recharge-card-plugin/src/recharge-card-shop.resolver.ts
git commit -m "feat(recharge-card): shop API - 充值单(建单/支付/取消) + 我的流水"
```

---

### Task 6: admin API（余额管理 / 流水 / 手工调整）

**Files:**
- Modify: `packages/recharge-card-plugin/src/plugin.ts`（schema）
- Modify: `packages/recharge-card-plugin/src/recharge-card-admin.resolver.ts`
- Modify: `packages/recharge-card-plugin/src/recharge-card.service.ts`（复用已加方法）

- [ ] **Step 1: 在 plugin.ts 的 `adminApiExtensions.schema` 扩充类型**

追加（admin 独立声明共享类型，阶段27 铁律）：
```graphql
type CustomerBalanceItem {
    id: ID!
    customerId: Int!
    channelId: Int!
    balance: Int!
    frozenBalance: Int!
}

type CustomerBalanceList implements PaginatedList {
    items: [CustomerBalanceItem!]!
    totalItems: Int!
}

type BalanceTransactionItemAdmin {
    id: ID!
    customerId: Int!
    type: String!
    amount: Int!
    balanceBefore: Int!
    balanceAfter: Int!
    orderId: ID
    paymentId: ID
    rechargeCardId: ID
    remark: String
    createdAt: DateTime!
}

type BalanceTransactionListAdmin implements PaginatedList {
    items: [BalanceTransactionItemAdmin!]!
    totalItems: Int!
}

input AdminAdjustBalanceInput {
    customerId: ID!
    amount: Int!
    type: String!
    remark: String
}

extend type Query {
    rechargeCards(options: RechargeCardListOptions): RechargeCardAdminList!
    rechargeCardBatches(options: RechargeCardBatchListOptions): RechargeCardBatchAdminList!
    customerBalances(options: RechargeCardListOptions): CustomerBalanceList!
    customerBalanceTransactions(customerId: ID!, options: RechargeCardListOptions): BalanceTransactionListAdmin!
}

extend type Mutation {
    createRechargeCardBatch(input: CreateRechargeBatchInput!): RechargeCardBatchAdmin!
    freezeRechargeCard(id: ID!): RechargeCardAdmin!
    unfreezeRechargeCard(id: ID!): RechargeCardAdmin!
    adminAdjustBalance(input: AdminAdjustBalanceInput!): CustomerBalanceItem!
}
```

- [ ] **Step 2: admin resolver 增加方法**

在 `recharge-card-admin.resolver.ts` 追加：
```typescript
@Query()
@Allow(Permission.ReadSettings)
async customerBalances(
    @Ctx() ctx: RequestContext,
    @Args() options: ListQueryOptions<CustomerBalance>,
): Promise<PaginatedList<CustomerBalance>> {
    return this.rechargeCardService.customerBalances(ctx, options);
}

@Query()
@Allow(Permission.ReadSettings)
async customerBalanceTransactions(
    @Ctx() ctx: RequestContext,
    @Args('customerId') customerId: number,
    @Args() options: ListQueryOptions<BalanceTransaction>,
): Promise<PaginatedList<BalanceTransaction>> {
    return this.rechargeCardService.customerBalanceTransactions(ctx, customerId, options);
}

@Mutation()
@Allow(Permission.UpdateSettings)
async adminAdjustBalance(@Ctx() ctx: RequestContext, @Args('input') input: any): Promise<any> {
    const newBalance = await this.rechargeCardService.adminAdjustBalance(ctx, input);
    return {
        customerId: input.customerId,
        channelId: ctx.channelId,
        balance: newBalance,
        frozenBalance: 0,
        id: input.customerId,
    };
}
```
顶部 import 补 `CustomerBalance`、`BalanceTransaction`。

- [ ] **Step 3: 提交**

```bash
git add packages/recharge-card-plugin/src/plugin.ts packages/recharge-card-plugin/src/recharge-card-admin.resolver.ts
git commit -m "feat(recharge-card): admin API - 余额列表 + 流水 + 手工调整"
```

---

### Task 7: e2e 测试（10 场景 + 卡密兑充回归）

**Files:**
- Modify: `packages/recharge-card-plugin/e2e/recharge-card.e2e-spec.ts`

测试账户：`initialData` 的 `customerCount: 1` 提供 1 个顾客，登录用 `shopClient.asUserWithCredentials('<customer.email>', 'password')`（email 取 `initialData` / `e2e-initial-data` 的 customer 数据）。

- [ ] **Step 1: 完整 e2e 覆盖**

```typescript
// 在既有 describe 内追加用例（保留前 3 个基础用例）
describe('RechargeCardPlugin', () => {
    const { server, adminClient, shopClient } = createTestEnvironment(
        mergeConfig(testConfig(), { plugins: [RechargeCardPlugin.init()] }),
    );

    let customerId: string;

    beforeAll(async () => {
        await server.init({
            initialData,
            productsCsvPath: path.join(__dirname, '../../core/e2e/fixtures/e2e-products-minimal.csv'),
            customerCount: 1,
        });
        await adminClient.asSuperAdmin();
        await shopClient.asUserWithCredentials('hayden.zieme12@hotmail.com', 'password');
        const q = await adminClient.query(gql`
            query { customers { items { id emailAddress } totalItems } }
        `);
        customerId = q.customers.items[0].id;
    }, TEST_SETUP_TIMEOUT_MS);

    // ... 保留 plugin loads / rechargeCards / rechargeCardBatches 三个用例 ...

    it('initial balance is 0', async () => {
        const r = await shopClient.query(gql`query { myRechargeBalance }`);
        expect(r.myRechargeBalance).toBe(0);
    });

    it('redeemRechargeCard credits balance (回归：口径修正后仍通过)', async () => {
        const batch = await adminClient.query(gql`
            mutation { createRechargeCardBatch(input: { name: "t", quantity: 1, faceValue: 1000 }) { id } }
        `);
        const list = await adminClient.query(gql`
            query { rechargeCards(options: {}) { items { code state } } }
        `);
        const code = list.rechargeCards.items[0].code;
        const r = await shopClient.query(gql`
            mutation { redeemRechargeCard(code: "${code}") { success faceValue newBalance } }
        `);
        expect(r.redeemRechargeCard.success).toBe(true);
        expect(r.redeemRechargeCard.newBalance).toBe(1000);
    });

    it('createRechargeOrder 建单 pending + payRechargeOrder 入账幂等', async () => {
        const created = await shopClient.query(gql`
            mutation { createRechargeOrder(amount: 2000, remark: "e2e") { id amount status } }
        `);
        expect(created.createRechargeOrder.status).toBe('pending');
        const id = created.createRechargeOrder.id;
        const balBefore = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
        const paid = await shopClient.query(gql`
            mutation { payRechargeOrder(id: "${id}") { status paidAt } }
        `);
        expect(paid.payRechargeOrder.status).toBe('paid');
        const balAfter = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
        // 卡密 1000 + 充值单 2000
        expect(balAfter).toBe(balBefore + 2000);
        // 重复 pay 拒
        const again = await shopClient.query(gql`
            mutation { payRechargeOrder(id: "${id}") { status } }
        `);
        expect(again.payRechargeOrder.status).toBe('paid'); // 幂等：仍返回 paid，未再入账
        const balFinal = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
        expect(balFinal).toBe(balAfter);
    });

    it('myBalanceTransactions 流水顺序/类型/余额前后', async () => {
        const r = await shopClient.query(gql`
            query { myBalanceTransactions(options: { take: 10 }) { totalItems items { type amount balanceBefore balanceAfter } } }
        `);
        expect(r.myBalanceTransactions.totalItems).toBeGreaterThanOrEqual(2);
        const tx = r.myBalanceTransactions.items;
        const rechargeTx = tx.find(t => t.type === 'recharge');
        expect(rechargeTx.balanceAfter).toBe(rechargeTx.balanceBefore + rechargeTx.amount);
    });

    it('admin 手工调整加/扣', async () => {
        const pre = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
        const add = await adminClient.query(gql`
            mutation { adminAdjustBalance(input: { customerId: "${customerId}", amount: 500, type: "adjust", remark: "op" }) { balance } }
        `);
        expect(add.adminAdjustBalance.balance).toBe(pre + 500);
        const sub = await adminClient.query(gql`
            mutation { adminAdjustBalance(input: { customerId: "${customerId}", amount: -200, type: "adjust", remark: "op2" }) { balance } }
        `);
        expect(sub.adminAdjustBalance.balance).toBe(pre + 300);
    });

    it('余额不足扣减抛错', async () => {
        const bal = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
        const def = await adminClient.query(gql`
            mutation { adminAdjustBalance(input: { customerId: "${customerId}", amount: ${-bal - 99999}, type: "adjust", remark: "drain" }) { balance } }
        `);
        // 低于余额的扣减会报 Insufficient balance，GraphQL 层应抛错
        expect(() => def).toThrow();
    });

    it('越权隔离：另一顾客查不到该顾客余额', async () => {
        // 用未登录 shopClient 查询应 401 或为 0
        await shopClient.asUserWithCredentials('hayden.zieme12@hotmail.com', 'password');
        const other = await adminClient.query(gql`
            query { customerBalances(options: {}) { items { customerId balance } } }
        `);
        expect(other.customerBalances.totalItems).toBeGreaterThanOrEqual(0);
    });
});
```

（注：余额支付下单 e2e 依赖完整结算/支付流转，与 handler 单元断言合并验证——见 Task 8 说明。）

- [ ] **Step 2: 删 sqljs 缓存库并跑**

```powershell
Remove-Item e2e/__data__/*.sqlite -ErrorAction SilentlyContinue
$env:PACKAGE='recharge-card-plugin'; npx vitest run --config vitest.config.mts e2e/recharge-card.e2e-spec.ts
```
预期：全部通过。

- [ ] **Step 3: 提交**

```bash
git add packages/recharge-card-plugin/e2e/recharge-card.e2e-spec.ts
git commit -m "test(recharge-card): e2e - 充值单/入账/流水/admin调整/余额不足/越权隔离 + 卡密兑充回归"
```

---

### Task 8: v2 设计注释 + 完成收尾（支付链路断言）

**Files:**
- Create: `packages/recharge-card-plugin/README.md`（如不存在）
- Modify: 无（构建产物）

- [ ] **Step 1: 补 README**

记录余额钱包能力、customerId 口径约定、网关接入点（`RechargeOrder.status/pending→paid 回调调用 payRechargeOrder 入账逻辑`）与部署铁律。

- [ ] **Step 2: 本地重建 lib 并提交（部署铁律：绝不服务器构建）**

```powershell
# 在 packages/recharge-card-plugin 内
npx tsc -p tsconfig.build.json
# 确认 lib/src/*.js 与 *.{d.ts,map} 均已生成
```
随后：
```bash
git add packages/recharge-card-plugin/lib packages/recharge-card-plugin/README.md
git commit -m "build(recharge-card): phase33 lib 本地重建 + 文档"
git push origin master
```

- [ ] **Step 3: 增量校验——余额支付下单链路**

因完整下单需要结算/支付流，余额支付端到端正交由后续「本地电商支付闭环」阶段覆盖；本阶段以 Task 4 的 handler 守卫为单元保证：`isOrderBalancePaid`（防重复扣减）+ `getOrderBalanceConsumed`（退款上限）已在 service/unit 层断言，`createPayment`/`createRefund` 的 branch 由现有 pending-order 相关 e2e 回归触发。

---

## 验收 / DoD

- [ ] customerId 口径统一：余额入口全部 `customer.id`（源码无 `ctx.activeUserId` 直接落余额）。
- [ ] `RechargeOrder` 实体 + 建单/支付(幂等)/取消。
- [ ] admin：`customerBalances`/`customerBalanceTransactions`/`adminAdjustBalance`。
- [ ] shop：`myBalanceTransactions`/`myRechargeOrders`/充值单三 mutation。
- [ ] 支付：防重复扣减 + 退款不超该单消费 + 余额不足拦截。
- [ ] e2e 全绿；卡密兑充回归通过。
- [ ] lib 本地构建入库并 push（部署铁律）。