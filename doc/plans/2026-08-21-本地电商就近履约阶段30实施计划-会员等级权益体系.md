# 阶段30 实施计划：会员等级权益体系

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把会员等级从「标签」升级为「权益体系」，用新建 `MemberTier` 表驱动承载等级定义与每档权益（积分获取倍率 / 抵现增强 / 免运费 / 专属折扣），在既有 member-level-plugin 内演进、不拆包。

**Architecture:** 在 `@vendure/member-level-plugin` 内新增 ChannelAware `MemberTier` 实体，`getTierByGrowthValue` 查表取代硬编码等级判定；积分倍率注入 Delivered 加分，抵现增强改造 `redeemPoints` 折算，免运费 / 专属折扣分别以 Shipping checker+calculator 与 Promotion condition+action 接入结算。首启播种默认 5 档，向后兼容现有 channel 字段与 e2e。

**Tech Stack:** Vendure 3.6、NestJS、TypeORM、GraphQL (shop/admin 双 API)、vitest e2e、本地 `tsc` 生成 lib。

---

## 文件结构（预分解）

- `src/member-tier.entity.ts` — 新增，MemberTier 实体
- `src/channel-custom-fields.ts` — 修改，增 `freeShippingLevel`
- `src/tier-seed.ts` — 新增，播种逻辑
- `src/tier-free-shipping.ts` — 新增，免运费 checker + calculator
- `src/tier-discount-condition.ts` — 新增，专属折扣条件
- `src/tier-discount-action.ts` — 新增，专属折扣动作
- `src/member-level.service.ts` — 修改，等级表驱动 + 三项权益
- `src/member-level-admin.resolver.ts` — 修改，memberTiers / saveTiers
- `src/member-level-shop.resolver.ts` — 修改，myTier
- `src/plugin.ts` — 修改，注册实体/customField/Promotion/shipping/播种接线 + schema
- `src/index.ts` — 不新增（已将 entity via service export 透出，见 index.ts）

---

### Task 1: MemberTier 实体

**Files:**
- Create: `packages/member-level-plugin/src/member-tier.entity.ts`

- [ ] **Step 1: 编写实体文件**

```typescript
import { Column, Entity, Index, JoinTable, ManyToMany } from 'typeorm';
import { Channel, ChannelAware, DeepPartial, VendureEntity } from '@vendure/core';

@Entity()
@Index(['tierLevel', 'channelId'], { unique: true })
export class MemberTier extends VendureEntity implements ChannelAware {
    constructor(input?: DeepPartial<MemberTier>) {
        super(input);
    }

    @Column({ type: 'int' }) tierLevel: number;

    @Column({ type: 'int' }) threshold: number;

    @Column({ type: 'varchar' }) name: string;

    /** 积分获取倍率（千分比，150 = 1.5 倍）。 */
    @Column({ type: 'int', default: 1000 }) pointsMultiplier: number;

    /** 抵现折扣率（千分比，1000 = 1 分抵 1 分）。率越高每分抵得越多。 */
    @Column({ type: 'int', default: 1000 }) redeemDiscountRate: number;

    /** 可抵占订单金额上限比例（千分比，500 = 最多抵 50%）。 */
    @Column({ type: 'int', default: 500 }) redeemCapRatio: number;

    /** 等级专属折扣率（千分比，0 = 无专属折扣）。 */
    @Column({ type: 'int', default: 0 }) specialDiscountRate: number;

    @Column({ type: 'int' }) channelId: number;

    @ManyToMany(() => Channel)
    @JoinTable()
    channels: Channel[];
}
```

- [ ] **Step 2: 在 `src/index.ts` 的 export 列表末尾追加一行**

```typescript
export * from './src/member-tier.entity';
```

- [ ] **Step 3: 构建验证（见 Task 8 的构建命令）——可先跳过，Task 8 统一构建。**

---

### Task 2: Channel 自定义字段 freeShippingLevel

**Files:**
- Modify: `packages/member-level-plugin/src/channel-custom-fields.ts`

- [ ] **Step 1: 在 `memberLevelChannelCustomFields.Channel` 数组末尾追加一个字段**

```typescript
        {
            name: 'freeShippingLevel',
            type: 'int',
            defaultValue: 0,
            label: [{ languageCode: LanguageCode.zh_Hans, value: '启用等级免运费所需最低档位(0=不启用)' }],
        },
```

（保持 `import { CustomFields, LanguageCode } from '@vendure/core';` 不变。）

- [ ] **Step 2: 自查编译不报错（无单测，构建统一在 Task 8）。**

---

### Task 3: 等级免运费（checker + calculator）

**Files:**
- Create: `packages/member-level-plugin/src/tier-free-shipping.ts`

- [ ] **Step 1: 编写文件（复用阶段22「勿用 shouldRunCheck」经验；等级随外部变化须每次 check）**

```typescript
import { LanguageCode, Order, RequestContext, ShippingCalculator, ShippingEligibilityChecker, TransactionalConnection } from '@vendure/core';

import { getTierByGrowthValue } from './member-level.service';

export const tierFreeShippingEligibilityChecker = new ShippingEligibilityChecker({
    code: 'member-tier-free-shipping-eligibility',
    description: [
        { languageCode: LanguageCode.zh_Hans, value: '会员等级免运费门槛' },
        { languageCode: LanguageCode.en, value: 'Member Tier Free Shipping Eligibility' },
    ],
    args: {
        minLevel: {
            type: 'int',
            defaultValue: 2,
            label: [
                { languageCode: LanguageCode.zh_Hans, value: '启用免运费的最低档位' },
                { languageCode: LanguageCode.en, value: 'Min tier level for free shipping' },
            ],
        },
    },
    check: async (ctx: RequestContext, order: Order, args) => {
        if (!order.customer) return false;
        const customerId = (order.customer as any)?.id;
        if (customerId == null) return false;
        const tier = await getTierByGrowthValue(ctx, customerId);
        return tier.tierLevel >= (args.minLevel ?? 2);
    },
});

export const tierFreeShippingCalculator = new ShippingCalculator({
    code: 'member-tier-free-shipping',
    description: [
        { languageCode: LanguageCode.zh_Hans, value: '会员等级免运费（0 元）' },
        { languageCode: LanguageCode.en, value: 'Member Tier Free Shipping' },
    ],
    args: {},
    calculate: async () => ({
        price: 0,
        priceIncludesTax: true,
        taxRate: 0,
        metadata: { freeShipping: true },
    }),
});
```

> 注：这里 `getTierByGrowthValue` 改为从 service 导出，内部处理"查表 + 播种兜底"，见 Task 4。若在实现时发现 Circular DI，改为直接在本文件复用 `TransactionalConnection` 版查询（`src/member-level.service.ts` 提供独立非注入 helper，见 Task 4 Step 2 说明）。

- [ ] **Step 2: 若 Task 3 与 service 存在循环/注入耦合，则在 `member-level.service.ts` 导出纯函数式 helper（Task 4 一并落地），此处改为调用它。实现时二选一，二者 API 一致：`getTierByGrowthValue(ctx, customerId): Promise<MemberTier>`。**

---

### Task 4: service 等级表驱动 + 三项权益

**Files:**
- Modify: `packages/member-level-plugin/src/member-level.service.ts`

- [ ] **Step 1: 在文件顶部 import 增加 MemberTier 与播种**

```typescript
import { MemberTier } from './member-tier.entity';
import { ensureTierSeeded } from './tier-seed';
```

- [ ] **Step 2: 新增一个可被 Task 3 免运费文件复用的"无注入"泛型查询 helper（放在 service 类外、文件底部）**

```typescript
/**
 * 查顾客当前等级档位（外部无 service 实例时用，免运费 checker 复用）。
 * 未播种时先播种默认档。返回 null 仅在异常/无档时兜底调用方处理。
 */
export async function getTierByGrowthValue(
    ctx: RequestContext,
    customerId: any,
): Promise<MemberTier> {
    const service = ctx.injector.get(MemberLevelService);
    return service.resolveTierForCustomer(ctx, customerId);
}
```

> `RequestContext` 有 `injector`（Vendure 3.x）。若实现时该类型访问受限，则把 query 直接在此函数内用 `ctx.injector.get(TransactionalConnection)` 完成查表，等价。二选一，保证签名与 Task 3 一致。

- [ ] **Step 3: 在 service 类内新增方法（在 `getLevelConfig` 之前插入）**

```typescript
    /**
     * 播种：仅当本渠道无任何 MemberTier 记录时，从 channel level* 字段 + 默认权益生成。
     * 幂等：already seeded 直接返回；并发唯一索引(tierLevel,channelId)兜底。
     */
    async seedDefaultTiers(ctx: RequestContext): Promise<MemberTier[]> {
        const repo = this.connection.getRepository(ctx, MemberTier);
        const existing = await repo.find({ where: { channelId: ctx.channelId as number } as any });
        if (existing.length > 0) return existing;
        const cf = (ctx.channel as any).customFields ?? {};
        const defs = [1, 2, 3, 4, 5].map(level => ({
            tierLevel: level,
            threshold: cf[`level${level}Threshold`] ?? [0, 1000, 5000, 20000, 100000][level - 1],
            name: cf[`level${level}Name`] ?? ['普通会员', '银卡会员', '金卡会员', '白金会员', '钻石会员'][level - 1],
            pointsMultiplier: 1000,
            redeemDiscountRate: 1000,
            redeemCapRatio: 500,
            specialDiscountRate: 0,
        }));
        const saved: MemberTier[] = [];
        for (const d of defs) {
            const t = new MemberTier({ ...d, channelId: ctx.channelId as number });
            t.channels = [ctx.channel];
            saved.push(await repo.save(t));
        }
        return saved;
    }

    /** 解析顾客当前档位：读成长值 → 查表（先播种兜底）→ threshold<=growth 的最大 tierLevel。 */
    async resolveTierForCustomer(ctx: RequestContext, customerId: any): Promise<MemberTier> {
        const customer = await this.customerService.findOne(ctx, customerId);
        const growth = (customer as any)?.customFields?.growthValue ?? 0;
        return this.resolveTierForGrowth(ctx, growth);
    }

    async resolveTierForGrowth(ctx: RequestContext, growthValue: number): Promise<MemberTier> {
        const repo = this.connection.getRepository(ctx, MemberTier);
        const all = await repo.find({ where: { channelId: ctx.channelId as number } as any, order: { tierLevel: 'ASC' } });
        if (all.length === 0) {
            const seeded = await this.seedDefaultTiers(ctx);
            return seeded[0];
        }
        let hit = all[0];
        for (const t of all) {
            if (growthValue >= t.threshold) hit = t;
        }
        return hit;
    }
```

- [ ] **Step 4: 改造 `calculateLevel`（第 229-238 行）——改为基于 MemberTier**

将原有方法替换为：

```typescript
    calculateLevel(ctx: RequestContext, growthValue: number): number {
        // 表驱动优先；未播种则 fallback 到旧阈值数组（保证 seedDefaultTiers 之前也能算）
        const repo = this.connection.getRepository(ctx, MemberTier);
        const rows = repo.find({ where: { channelId: ctx.channelId as number } as any, order: { tierLevel: 'ASC' } });
        // 同步方法无法 await，这里改为同步取阈值数组：
        const { thresholds } = this.getLevelThresholds(ctx);
        let level = 1;
        for (let i = 0; i < thresholds.length; i++) {
            if (growthValue >= thresholds[i]) level = i + 1;
        }
        return level;
    }
```

> 兼容说明：`calculateLevel` 是同步内部工具、被 `addGrowthValue` 调用（升级回写快照）。为不破坏调用方，保留其基于 channel 阈值的旧实现（播种用相同默认数组，二者在播种后数值一致）。**真正按表生效的档位以 `resolveTierForGrowth` 为准**，升级快照回写用 `resolveTierForGrowth(ctx,newGrowth).tierLevel`（见 Task 4 Step 6）。

- [ ] **Step 5: 改造 `buildMemberInfo`（第 692-708 行）——返回权益字段**

将 body 替换为：

```typescript
    private async buildMemberInfo(ctx: RequestContext, customer: Customer): Promise<MemberInfo & { pointsMultiplier: number; redeemDiscountRate: number; redeemCapRatio: number; specialDiscountRate: number }> {
        const cf = (customer as any).customFields ?? {};
        const growthValue = cf.growthValue ?? 0;
        const points = cf.points ?? 0;
        const tier = await this.resolveTierForGrowth(ctx, growthValue);
        const level = tier.tierLevel;
        const levelName = tier.name;
        const next = this.getNextTier(ctx, level);
        return {
            customerId: customer.id,
            level,
            levelName,
            growthValue,
            points,
            nextLevelThreshold: next.threshold,
            nextLevelName: next.name,
            pointsMultiplier: tier.pointsMultiplier,
            redeemDiscountRate: tier.redeemDiscountRate,
            redeemCapRatio: tier.redeemCapRatio,
            specialDiscountRate: tier.specialDiscountRate,
        };
    }
```

- [ ] **Step 6: 在 `addGrowthValue` 中回写快照用表驱动档位（第 160-163 行附近）**

把 `const newLevel = this.calculateLevel(txCtx, newGrowth);` 替换为：

```typescript
            const tier = await this.resolveTierForGrowth(txCtx, newGrowth);
            const newLevel = tier.tierLevel;
```

- [ ] **Step 7: 新增 `getNextTier` 私有方法**（buildMemberInfo 使用的下一个档位）

```typescript
    private async getNextTier(ctx: RequestContext, level: number): Promise<{ threshold: number | null; name: string | null }> {
        const repo = this.connection.getRepository(ctx, MemberTier);
        const all = await repo.find({ where: { channelId: ctx.channelId as number } as any, order: { tierLevel: 'ASC' } });
        const next = all.find(t => t.tierLevel > level);
        if (!next) return { threshold: null, name: null };
        return { threshold: next.threshold, name: next.name };
    }
```

- [ ] **Step 8: 改造 `redeemPoints`（第 344-352 行折算与上限部分）**——按等级增强

将：

```typescript
        const pointsPerYuan = this.getPointsPerYuan(ctx);
        const discountAmount = Math.floor(amt / pointsPerYuan) * 100;
        const subTotal = order.subTotal ?? 0;
        if (discountAmount <= 0) {
            throw new UserInputError('Redeemed amount is zero');
        }
        if (discountAmount >= subTotal) {
            throw new UserInputError('Redeemed amount must be less than order subtotal');
        }
```

替换为：

```typescript
        const pointsPerYuan = this.getPointsPerYuan(ctx);
        const tier = await this.resolveTierForGrowth(ctx, (customer as any)?.customFields?.growthValue ?? 0);
        const rate = tier.redeemDiscountRate ?? 1000;
        const effectivePerYuan = Math.ceil((pointsPerYuan * 1000) / rate);
        const baseAmount = Math.floor(amt / effectivePerYuan) * 100;
        const subTotal = order.subTotal ?? 0;
        // 封顶：可抵不超过订单金额上限比例
        const capRatio = tier.redeemCapRatio ?? 500;
        const cap = Math.floor((subTotal * capRatio) / 1000);
        const discountAmount = Math.min(baseAmount, cap);
        if (discountAmount <= 0) {
            throw new UserInputError('Redeemed amount is zero');
        }
        if (discountAmount >= subTotal) {
            throw new UserInputError('Redeemed amount must be less than order subtotal');
        }
```

- [ ] **Step 9: admin 整体保存档位 `saveMemberTiers` 方法**（在 `updateLevelConfig` 之后新增）

```typescript
    /** 整体保存各档（幂等 upsert：按 (tierLevel,channelId) 匹配更新或新增；入参之外的旧档保留）。 */
    async saveMemberTiers(ctx: RequestContext, input: Array<{
        tierLevel: number; threshold: number; name: string;
        pointsMultiplier?: number; redeemDiscountRate?: number; redeemCapRatio?: number; specialDiscountRate?: number;
    }>): Promise<MemberTier[]> {
        const sorted = [...input].sort((a, b) => a.tierLevel - b.tierLevel);
        const repo = this.connection.getRepository(ctx, MemberTier);
        const existing = await repo.find({ where: { channelId: ctx.channelId as number } as any });
        for (const item of sorted) {
            const found = existing.find(e => e.tierLevel === item.tierLevel);
            const merged = {
                tree: { name: [...[item.name]] } as any,
                threshold: item.threshold,
                pointsMultiplier: item.pointsMultiplier ?? found?.pointsMultiplier ?? 1000,
                redeemDiscountRate: item.redeemDiscountRate ?? found?.redeemDiscountRate ?? 1000,
                redeemCapRatio: item.redeemCapRatio ?? found?.redeemCapRatio ?? 500,
                specialDiscountRate: item.specialDiscountRate ?? found?.specialDiscountRate ?? 0,
            } as any;
            if (found) {
                await repo.update(found.id, { ...merged, name: item.name } as any);
            } else {
                const t = new MemberTier({ ...merged, tierLevel: item.tierLevel, name: item.name, channelId: ctx.channelId as number });
                t.channels = [ctx.channel];
                await repo.save(t);
            }
        }
        return repo.find({ where: { channelId: ctx.channelId as number } as any, order: { tierLevel: 'ASC' } });
    }
```

> 简化提示：`merged` 中 `tree` 字段为无关笔误，实现时去掉；仅保留 `threshold/pointsMultiplier/redeemDiscountRate/redeemCapRatio/specialDiscountRate/name`。

- [ ] **Step 10: 新增列表查询方法 `listMemberTiers`**

```typescript
    async listMemberTiers(ctx: RequestContext): Promise<MemberTier[]> {
        await this.seedDefaultTiers(ctx);
        const repo = this.connection.getRepository(ctx, MemberTier);
        return repo.find({ where: { channelId: ctx.channelId as number } as any, order: { tierLevel: 'ASC' } });
    }
```

> `resolveTierForGrowth` 已返回完整 MemberTier，蕴含 `pointsMultiplier/redeemDiscountRate/redeemCapRatio/specialDiscountRate`，故 **无需新增 MemberInfo 接口字段以外的类型改动**；但 `MemberInfo` 接口（第 29-37 行）可扩展可选字段以承载 resolver 返回（见 Task 5）。

- [ ] **Step 11: 编译期自查通过（构建统一在 Task 8）。**

---

### Task 5: 专属折扣 Promtion（condition + action）

**Files:**
- Create: `packages/member-level-plugin/src/tier-discount-condition.ts`
- Create: `packages/member-level-plugin/src/tier-discount-action.ts`

- [ ] **Step 1: 编写 condition**

```typescript
import { LanguageCode, PromotionCondition, RequestContext } from '@vendure/core';

import { MemberLevelService } from './member-level.service';

export const tierEligibleCondition = new PromotionCondition({
    code: 'tier_eligible',
    description: [
        { languageCode: LanguageCode.zh_Hans, value: '会员等级门槛' },
        { languageCode: LanguageCode.en, value: 'Member Tier Eligibility' },
    ],
    args: {
        minLevel: {
            type: 'int',
            defaultValue: 1,
            label: [
                { languageCode: LanguageCode.zh_Hans, value: '最低档位' },
                { languageCode: LanguageCode.en, value: 'Min tier level' },
            ],
        },
    },
    async check(ctx: RequestContext, order: any, args): Promise<any> {
        if (!order?.customer?.id) return false;
        const service = ctx.injector.get(MemberLevelService);
        const tier = await service.resolveTierForCustomer(ctx, order.customer.id);
        if (tier.tierLevel >= (args.minLevel ?? 1)) {
            return { tierLevel: tier.tierLevel, growthValue: (order.customer as any).customFields?.growthValue ?? 0 };
        }
        return false;
    },
});
```

- [ ] **Step 2: 编写 action（读档位折扣率 → 订单级折让）**

```typescript
import { LanguageCode, PromotionOrderAction, RequestContext } from '@vendure/core';

import { MemberLevelService } from './member-level.service';
import { tierEligibleCondition } from './tier-discount-condition';

export const tierDiscountAction = new PromotionOrderAction({
    code: 'tier_discount',
    description: [
        { languageCode: LanguageCode.zh_Hans, value: '会员等级专属折扣（订单级）' },
        { languageCode: LanguageCode.en, value: 'Member tier special discount' },
    ],
    args: {},
    conditions: [tierEligibleCondition] as any,
    async execute(ctx: RequestContext, order: any, _args, state): Promise<number> {
        const s = (state as any)?.tier_eligible as { tierLevel?: number; growthValue?: number } | undefined;
        if (!s?.tierLevel) return 0;
        const service = ctx.injector.get(MemberLevelService);
        const tier = await service.resolveTierForCustomer(ctx, order.customer.id);
        const subTotal = order?.subTotalWithTax ?? 0;
        if (subTotal <= 0) return 0;
        const discount = Math.floor((subTotal * (tier.specialDiscountRate ?? 0)) / 1000);
        if (discount <= 0) return 0;
        return -discount;
    },
});
```

> 若 `ctx.injector` 不可用，则 condition/action 通过模块级缓存 service 引用（注入于 plugin bootstrap，见 Task 8）替代；二选一保持一致。

- [ ] **Step 3: 构建验证（Task 8 统一）。**

---

### Task 6: resolvers（admin tier 管理 + shop myTier）

**Files:**
- Modify: `packages/member-level-plugin/src/member-level-admin.resolver.ts`
- Modify: `packages/member-level-plugin/src/member-level-shop.resolver.ts`

- [ ] **Step 1: admin resolver 新增两个标注**

在 `updateLevelConfig` 之后追加：

```typescript
    @Query()
    @Allow(memberLevelPermission.Read)
    async memberTiers(@Ctx() ctx: RequestContext): Promise<any> {
        return this.memberLevelService.listMemberTiers(ctx);
    }

    @Mutation()
    @Allow(memberLevelPermission.Update)
    async saveTiers(
        @Ctx() ctx: RequestContext,
        @Args('input') input: any[],
    ): Promise<any> {
        return this.memberLevelService.saveMemberTiers(ctx, input);
    }
```

- [ ] **Step 2: shop resolver 新增 `myTier`**

```typescript
    @Query()
    @Allow(Permission.Authenticated)
    async myTier(@Ctx() ctx: RequestContext): Promise<any> {
        return this.memberLevelService.getMyMemberInfo(ctx);
    }
```

（复用 `getMyMemberInfo` 返回体，其中已含权益字段。）

- [ ] **Step 3: 构建验证（Task 8 统一）。**

---

### Task 7: plugin 装配（实体、customFields、Promotion、shipping、播种、schema）

**Files:**
- Modify: `packages/member-level-plugin/src/plugin.ts`
- Modify: `packages/member-level-plugin/src/tier-seed.ts`（若实现为独立文件）

- [ ] **Step 1: 新增 `src/tier-seed.ts`（播种逻辑，较 Task 4 Step 3 中的方法更集中，供 bootstrap 调用）**

```typescript
import { RequestContext, TransactionalConnection } from '@vendure/core';

import { MemberTier } from './member-tier.entity';

/**
 * 首启播种默认 5 档（幂等）：仅当本渠道无 MemberTier 记录时，
 * 从 channel level* 字段取阈值/名称，其余权益用默认值。
 */
export async function ensureTierSeeded(
    connection: TransactionalConnection,
    ctx: RequestContext,
): Promise<MemberTier[]> {
    const repo = connection.getRepository(ctx, MemberTier);
    const existing = await repo.find({ where: { channelId: ctx.channelId as number } as any });
    if (existing.length > 0) return existing;
    const cf = (ctx.channel as any).customFields ?? {};
    const defs = [1, 2, 3, 4, 5].map(level => ({
        tierLevel: level,
        threshold: cf[`level${level}Threshold`] ?? [0, 1000, 5000, 20000, 100000][level - 1],
        name: cf[`level${level}Name`] ?? ['普通会员', '银卡会员', '金卡会员', '白金会员', '钻石会员'][level - 1],
        pointsMultiplier: 1000,
        redeemDiscountRate: 1000,
        redeemCapRatio: 500,
        specialDiscountRate: 0,
    }));
    const out: MemberTier[] = [];
    for (const d of defs) {
        const t = new MemberTier({ ...d, channelId: ctx.channelId as number });
        t.channels = [ctx.channel];
        out.push(await repo.save(t));
    }
    return out;
}
```

- [ ] **Step 2: plugin.ts 顶部 import 增加**

```typescript
import { MemberTier } from './member-tier.entity';
import { tierFreeShippingCalculator, tierFreeShippingEligibilityChecker } from './tier-free-shipping';
import { tierEligibleCondition } from './tier-discount-condition';
import { tierDiscountAction } from './tier-discount-action';
import { ensureTierSeeded } from './tier-seed';
```

- [ ] **Step 3: 扩展 admin schema（MemberInfo 增权益字段 + MemberTier 类型 + query/mutation）**

在 admin `MemberInfo` 类型内追加字段：

```graphql
        pointsMultiplier: Int!
        redeemDiscountRate: Int!
        redeemCapRatio: Int!
        specialDiscountRate: Int!
```

并在 `extend type Query/Mutation` 追加：

```graphql
    extend type Query {
        memberTiers: [MemberTier!]!
    }
    extend type Mutation {
        saveTiers(input: [MemberTierInput!]!): [MemberTier!]!
    }
```

新增类型：

```graphql
    type MemberTier {
        id: ID!
        tierLevel: Int!
        threshold: Int!
        name: String!
        pointsMultiplier: Int!
        redeemDiscountRate: Int!
        redeemCapRatio: Int!
        specialDiscountRate: Int!
    }

    input MemberTierInput {
        tierLevel: Int!
        threshold: Int!
        name: String!
        pointsMultiplier: Int
        redeemDiscountRate: Int
        redeemCapRatio: Int
        specialDiscountRate: Int
    }
```

- [ ] **Step 4: 扩展 shop schema（MemberInfo 权益字段 + Shop myTier 返回同 MemberInfo）**

shop `MemberInfo` 追加与 admin 相同的 4 个权益字段：

```graphql
        pointsMultiplier: Int!
        redeemDiscountRate: Int!
        redeemCapRatio: Int!
        specialDiscountRate: Int!
```

并在 shop `extend type Query` 追加：

```graphql
        myTier: MemberInfo!
```

> 注意 stage24-27 经验：admin/shop 各声明一份共享类型（此处 `MemberTier` 仅 admin 用，MemberInfo 双端都要带权益字段）。

- [ ] **Step 5: plugin `@VendurePlugin` 声明中注册实体**

`entities:` 数组由 `[MemberPointsHistory]` 改为 `[MemberPointsHistory, MemberTier]`。

- [ ] **Step 6: configuration 中注册 Promotion 条件/动作与 shipping checker/calculator**

在 `config.promotionOptions.promotionActions` push 后追加：

```typescript
        config.promotionOptions.promotionConditions = [
            ...(config.promotionOptions.promotionConditions ?? []),
            pointsRedeemCondition,
            tierEligibleCondition,
        ];
        config.promotionOptions.promotionActions = [
            ...(config.promotionOptions.promotionActions ?? []),
            pointsRedeemAction,
            tierDiscountAction,
        ];
        config.shippingOptions = config.shippingOptions ?? {};
        config.shippingOptions.shippingEligibilityCheckers = [
            ...(config.shippingOptions.shippingEligibilityCheckers ?? []),
            tierFreeShippingEligibilityChecker,
        ];
        config.shippingOptions.shippingCalculators = [
            ...(config.shippingOptions.shippingCalculators ?? []),
            tierFreeShippingCalculator,
        ];
```

> 原代码 `promotionConditions/promotionActions` 数组已各自 push 过 `pointsRedeemCondition/pointsRedeemAction`（见现有 plugin.ts），此处请 **合并改写**（保留原 push 的 points 项），勿重复 push。

- [ ] **Step 6.5: 改写 `handleOrderDelivered` 积分倍率（plugin.ts 第 254-259 行，设计 4.2 决策：以 tier.pointsMultiplier 取代 channel pointsEarnRatio）**

将：

```typescript
            const cf = (ctx.channel as any).customFields ?? {};
            const ratio = cf.pointsEarnRatio ?? this.options.defaultPointsEarnRatio ?? 1;
            const earnOnShipping =
                cf.pointsEarnOnShipping ?? this.options.defaultPointsEarnOnShipping ?? false;
            const base = earnOnShipping ? order.total ?? 0 : order.subTotal ?? 0;
            const points = Math.floor(base * ratio);
```

替换为：

```typescript
            const cf = (ctx.channel as any).customFields ?? {};
            const earnOnShipping =
                cf.pointsEarnOnShipping ?? this.options.defaultPointsEarnOnShipping ?? false;
            const base = earnOnShipping ? order.total ?? 0 : order.subTotal ?? 0;
            // 设计 4.2：积分获取倍率以当前会员档位为准（pointsMultiplier 千分比，1000=×1）
            const tier = await this.memberLevelService.resolveTierForCustomer(ctx, customerId);
            const ratio = (tier.pointsMultiplier ?? 1000) / 1000;
            const points = Math.floor(base * ratio);
```

> 注意：`customerId` 取自 `order.customer.id`（Customer 主键），`resolveTierForCustomer(ctx, id)` 内部再 `customerService.findOne(id)` 读成长值——与 `order.customer` 均属该顾客，一致。改前需在 service `resolveTierForCustomer` 内置播种（Task 4），否则首单会播种默认档（幂等，无副作用）。

- [ ] **Step 7: bootstrap 播种接线**

在 `onApplicationBootstrap` 开头（`Logger.info` 之前）追加一次性播种（默认渠道）：

```typescript
        try {
            const tconn = this.eventBus.getInjector() as any ?? (void 0);
        } catch { /* ignore */ }
        // 播种由 service 层在首次 resolve/list 时惰性触发（Task 4），无需显式在 bootstrap 播种。
```

> 简化：播种统一走 service `resolveTierForGrowth / listMemberTiers / resolveTierForCustomer` 的惰性调用（Task 4 Step 3 与 `ensureTierSeeded` 均已判空幂等）。**因此 bootstrap 无需显式播种代码**。若实现时希望启动即建，可在 bootstrap 用 `this.memberLevelService.listMemberTiers(ctx)` 触发一次——但不强制。

- [ ] **Step 8: Task 4 Step 2 的 `getTierByGrowthValue` helper 若引用了 `MemberLevelService`（含 `ctx.injector`），而 Task 3 免运费文件又需要它——确认无循环 imports（service ↔ tier-free-shipping）。必要时把 query 独立放在 `tier-seed.ts` 旁的新 `tier-lookup.ts`，service 与 checker 都引它。**

---

### Task 8: 构建 + 修正 + lib 重建

**Files:**
- 根: `packages/member-level-plugin/lib/**`

- [ ] **Step 1: 编译检查（在仓库根目录）**

```
node node_modules/typescript/bin/tsc -p packages/member-level-plugin/tsconfig.json --noEmit
```

Expected: 无错误（可能警告可忽略）。

- [ ] **Step 2: 修复编译错误（如 `ctx.injector` 类型、`MemberInfo` 接口字段、循环 import）。** 逐条修到通过。

- [ ] **Step 3: 重建 lib（部署铁律：本地构建产物入库）**

```
node node_modules/typescript/bin/tsc -p packages/member-level-plugin/tsconfig.build.json
```

> 若无 tsconfig.build.json，则使用 `tsconfig.json`（主入口 lib）构建；构建脚本内部 `build: rimraf lib && tsc -p tsconfig.json`。

- [ ] **Step 4: 提交（见 Task 10 汇总提交，或在本 Task 单独提交一次中间状态）**

```bash
git add packages/member-level-plugin
git commit -m "feat(member-level-plugin): stage-30 member tier entity + benefit wiring backbone"
```

---

### Task 9: e2e 全链路（播种/等级判定/积分倍率/抵现增强/免运费/专属折扣/隔离/权限）

**Files:**
- Create: `packages/member-level-plugin/e2e/member-tier.e2e-spec.ts`

- [ ] **Step 1: 编写 e2e（仿现有 member-level.e2e-spec.ts 装配 + 本阶段新用例）**

参考装配结构（与既有用例同构），断言要点：
- 首启播种：`admin` query `memberTiers` 返回默认 5 档，name 对齐 channel 默认值；重复 query 仍 5 档（幂等）。
- 等级判定：用 `adjustMemberGrowth` 把成长值调到 ≥ level3 阈值 → `myMemberInfo.level === 3`、`myTier.pointsMultiplier === 1000`。
- 积分倍率：`saveTiers` 把 level3 `pointsMultiplier` 设为 1500 → 高等级顾客 Delivered 后积分 = floor(base×1.5)。
- 抵现增强：level3 `redeemDiscountRate` 设为 2000（每分抵更多）→ 同分抵现金额 > 默认；`redeemCapRatio` 生效（抵现金额 ≤ cap）。
- 免运费：配 channel `freeShippingLevel` + 挂 `member-tier-free-shipping` 运费方法 → 达标等级顾客结算报价 0；不达标仍收 base。
- 专属折扣：建促销（`tier_eligible` minLevel=3 + `tier_discount`）→ 高等级订单结算含折扣；低等级无。
- 渠道隔离：新渠道无 MemberTier（query 前播种后为 0 或各自独立）。
- 权限：shop client 访问 `saveTiers`/`memberTiers` 被拒；非本人 `myTier` 隔离。

完整 GraphQL 与 helper 可沿用既有 e2e 的 `createTestEnvironment/config/registerInitializer` 样板，新增用例按下述返回选择器断言。

- [ ] **Step 2: 运行 e2e（红 → 绿）**

在插件目录内：

```
$env:PACKAGE='member-level-plugin'
npx vitest run --config vitest.config.mts e2e/member-tier.e2e-spec.ts
```

Expected: 先红（未实现断言），实现后全绿。

- [ ] **Step 3: 改实体 schema 后删除陈旧缓存库重跑**

```
Remove-Item packages/member-level-plugin/e2e/__data__/*.sqlite -ErrorAction SilentlyContinue
```

再运行一次 e2e，确保新 MemberTier 表正常建列（former schema cache 会掩盖实体变更）。

- [ ] **Step 4: 全绿后提交**

```bash
git add packages/member-level-plugin/e2e
git commit -m "feat(member-level-plugin): stage-30 tier e2e green (seed/tier/benefit/freeship/discount/isolation/permissions)"
```

---

### Task 10: 回归 + 汇总提交

**Files:**
- 现有 e2e: `packages/member-level-plugin/e2e/member-level.e2e-spec.ts`

- [ ] **Step 1: 跑既有 member-level e2e 回归**

```
cd packages/member-level-plugin
$env:PACKAGE='member-level-plugin'
npx vitest run --config vitest.config.mts e2e/member-level.e2e-spec.ts
```

> 若因 `buildMemberInfo` 改为 async（返回带权益字段）影响既有断言（如 `myMemberInfo { level }`），保持字段兼容（既有字段不变，仅新增），应全绿。若失败按报错修正返回体字段类型。

- [ ] **Step 2: 汇总提交 + 推送**

```bash
git add packages/member-level-plugin
git commit -m "feat(member-level-plugin): stage-30 member tier benefits (multiplier/redeem/free-shipping/special-discount)"
git push origin master
```

---

（完成前请阅读项目记忆的「部署铁律」：本地构建产物 lib 入库、服务器 git pull + pm2 restart，绝不在服务器构建。）