# 阶段34 支付网关升级 + 会员在线充值 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans 逐任务实现。任务用 `- [ ]` 复选框跟踪。

**Goal:** 把 `wechatpay-plugin` 从「仅结 Vandure Order」升级为按 `out_trade_no` 结算的通用支付网关，并打通会员在线充值闭环（微信支付 → 充值单入账 → 余额到账）。本期只做后端 + GraphQL API，C 端页面下一阶段。

**Architecture:** wechatpay-plugin 提供 `WechatpayService.createBarePayment`（按业务号建微信支付，不依赖 Order）与 `notify` 的 `WechatpaySettlementRegistry` 结算路由（按 out_trade_no 前缀匹配，无匹配默认结 Order，向后兼容）。recharge-card-plugin 依赖 wechatpay（依赖方向：钱包→网关），注册 `RC-` 前缀结算处理器，在线充值时前端 `createWechatRechargePayment(rechargeOrderId)` 拿支付参数，微信回调落到网关 → `settleRechargeOrderByOutTradeNo` → 原子入账 + 记 `RECHARGE` 流水。

**Tech Stack:** TypeScript / NestJS / Vendure Plugin、`wechatpay-node-v3`、GraphQL、Vitest + `@vendure/testing`、sqljs。

---
## 设计要点（已评审通过）

1. **网关（wechatpay-plugin）**
   - `WechatpayService`：集中构造 WxPay 实例+凭证（复用 `getPaymentOverride`）、`createBarePayment(input)` 返回 JSAPI/H5/NATIVE/APP 任一支付参数；注入 `WECHATPAY_PLUGIN_OPTIONS` 感知 `devBypass`/`notifyUrl`。
   - `WechatpaySettlementRegistry`：`register(settlement)` / `find(outTradeNo)`。
   - 通用 shop mutation `wechatpayCreatePayment(input)`。
   - `WechatpayPlugin` import 安全化（`options ?? {}`、`onApplicationBootstrap` 守卫），供其它插件无条件 import。
   - controller `notify`/`dev-notify`：解码 `out_trade_no` → 先查 registry → 命中调 `settle`，否则回退 `settleOrderPayment`（现行为）。`dev-pay`/`dev-notify` 参数改 `outTradeNo`。

2. **充值接入（recharge-card-plugin）**
   - `RechargeOrder.externalRef` 新列（记 `out_trade_no`，幂等核对）。
   - shop mutation `createWechatRechargePayment(rechargeOrderId, tradeType?)`：校验本人 pending 单 → `createBarePayment({ outTradeNo: \`RC-${id}\`, amount, tradeType, description })` → 回写 `paymentMethod='wechatpay'` + `externalRef` → 返回支付参数。
   - `WechatpaySettlementRegistry` 注册 `RC-` → `settleRechargeOrderByOutTradeNo(outTradeNo)`：解析 `RC-<id>` 加载单，原子 `pending→paid`（claim.affected===0 跳过）后按 `order.customerId` 显式入账（admin ctx 无 activeUser，用单归属），记 `RECHARGE`。

3. **安全/一致**：回调验签/解密沿用现有逻辑；重复/并发入账靠原子 `UPDATE ... WHERE status='pending'`；`createWechatRechargePayment` 仅本人 pending 单可建；结算走 admin ctx，不暴露 shop；`JSAPI` 需 openid（前端传），devBypass 下返回 dev 支付页。

4. **回归**：现有 recharge-card 11 例继续通过（wechatpay 已 import 安全）；wechatpay 默认 Order 结算逻辑不变。

---
## 文件结构

**wechatpay-plugin**
- Create: `src/wechatpay.service.ts`（WechatpayService：配置构造 + createBarePayment + devBypass）
- Create: `src/wechatpay-settlement.ts`（WechatpaySettlement 接口 + WechatpaySettlementRegistry）
- Modify: `src/plugin.ts`（providers 增 service/registry；shop 扩展 schema + mutation resolver；onApplicationBootstrap 守卫）
- Create: `src/wechatpay-shop.resolver.ts`（wechatpayCreatePayment）
- Modify: `src/wechatpay.controller.ts`（notify/dev-notify 走 registry 路由；dev-pay 改 outTradeNo）
- Modify: `index.ts`（导出 service/settlement）
- Create: `e2e/wechatpay.e2e-spec.ts`

**recharge-card-plugin**
- Modify: `src/recharge-order.entity.ts`（+ `externalRef`）
- Modify: `src/recharge-card.service.ts`（+ `createWechatRechargePayment`、`settleRechargeOrderByOutTradeNo`）
- Modify: `src/plugin.ts`（imports 增 WechatpayPlugin；resolver 增；onApplicationBootstrap 注册 RC- 结算；schema 增 mutation/type）
- Modify: `e2e/recharge-card.e2e-spec.ts`（plugins 增 `WechatpayPlugin.init({devBypass:true})`；加充值支付用例）

---

## Task 1: wechatpay-plugin 网关服务 + 结算注册表

**Files:**
- Create: `packages/wechatpay-plugin/src/wechatpay-settlement.ts`
- Create: `packages/wechatpay-plugin/src/wechatpay.service.ts`
- Modify: `packages/wechatpay-plugin/index.ts`

- [ ] **Step 1: 建 settlement 类型与注册表**

创建 `packages/wechatpay-plugin/src/wechatpay-settlement.ts`：

```ts
import { RequestContext } from '@vendure/core';
import { Injectable } from '@nestjs/common';

/** 基于 out_trade_no 的非订单支付结算器 */
export interface WechatpaySettlement {
    /** out_trade_no 前缀，如 'RC-' */
    readonly prefix: string;
    settle(ctx: RequestContext, outTradeNo: string, transactionId: string): Promise<void>;
}

@Injectable()
export class WechatpaySettlementRegistry {
    private handlers: WechatpaySettlement[] = [];
    register(h: WechatpaySettlement): void {
        if (!this.handlers.some(x => x.prefix === h.prefix)) {
            this.handlers.push(h);
        }
    }
    find(outTradeNo: string): WechatpaySettlement | undefined {
        return this.handlers.find(h => outTradeNo.startsWith(h.prefix));
    }
}
```

- [ ] **Step 2: 建 WechatpayService**

创建 `packages/wechatpay-plugin/src/wechatpay.service.ts`：

```ts
import { Inject, Injectable } from '@nestjs/common';
import {
    ChannelService,
    Logger,
    PaymentMethodService,
    RequestContext,
} from '@vendure/core';
import crypto from 'crypto';
import WxPay from 'wechatpay-node-v3';
import { getPaymentOverride } from '@vendure/cjk-plugin';
import type { WechatpayCredentials } from '@vendure/cjk-plugin';

import { WECHATPAY_PLUGIN_OPTIONS, loggerCtx } from './constants';
import { WechatpayPluginOptions } from './types';

export interface BarePaymentInput {
    outTradeNo: string;
    /** 金额：分 */
    amount: number;
    tradeType?: 'JSAPI' | 'NATIVE' | 'H5' | 'APP';
    openid?: string;
    description?: string;
}

export interface BarePaymentResult {
    payType: string;
    prepayId?: string;
    appId?: string;
    timeStamp?: string;
    nonceStr?: string;
    package?: string;
    signType?: string;
    paySign?: string;
    payUrl?: string;
}

@Injectable()
export class WechatpayService {
    constructor(
        @Inject(WECHATPAY_PLUGIN_OPTIONS) private options: WechatpayPluginOptions,
        private channelService: ChannelService,
        private paymentMethodService: PaymentMethodService,
    ) {}

    /** 集中构造配置好的 WxPay 实例 + 凭证（复用 getPaymentOverride） */
    private async buildWechatpay(): Promise<{
        pay: WxPay;
        appId: string;
        privateKey: string;
        tradeType: string;
    }> {
        const channel = await this.channelService.getDefaultChannel();
        const ctx = new RequestContext({
            apiType: 'admin',
            channel,
            isAuthorized: true,
            authorizedAsOwnerOnly: false,
        });
        const override = getPaymentOverride(ctx, 'wechatpay') as WechatpayCredentials | null;
        const pms = await this.paymentMethodService.findAll(ctx);
        const pm = pms.items.find(p => p.code === 'wechatpay');
        const args = pm?.handler?.args || [];
        const getArg = (name: string) => args.find(a => a.name === name)?.value || '';
        const appId = override?.appId || getArg('appId');
        const privateKey = override?.privateKey || getArg('privateKey');
        return {
            pay: new WxPay({
                appid: appId,
                mchid: override?.mchId || getArg('mchId'),
                publicKey: Buffer.from(override?.publicKey || getArg('publicKey')),
                privateKey: Buffer.from(privateKey),
                key: override?.apiKey || getArg('apiKey'),
                serial_no: override?.serialNo || getArg('serialNo'),
            }),
            appId,
            privateKey,
            tradeType: override?.tradeType || getArg('tradeType') || 'JSAPI',
        };
    }

    /** devBypass 下返回模拟支付页；否则调真实微信 API 生成支付参数 */
    async createBarePayment(input: BarePaymentInput): Promise<BarePaymentResult> {
        if (this.options?.devBypass) {
            return {
                payType: 'dev-h5',
                payUrl: `/wechatpay/dev-pay?outTradeNo=${encodeURIComponent(input.outTradeNo)}`,
            };
        }
        const { pay, appId, privateKey, tradeType } = await this.buildWechatpay();
        const baseParams = {
            description: input.description || `Pay ${input.outTradeNo}`,
            out_trade_no: input.outTradeNo,
            notify_url: this.options?.notifyUrl || '',
            amount: { total: Math.round(input.amount / 100), currency: 'CNY' },
        };
        const type = input.tradeType || tradeType;

        if (type === 'NATIVE') {
            const r = (await pay.transactions_native(baseParams)) as any;
            return { payType: 'native', payUrl: r?.data?.code_url };
        }
        if (type === 'H5') {
            const r = (await pay.transactions_h5({
                ...baseParams,
                scene_info: {
                    payer_client_ip: '127.0.0.1',
                    h5_info: { type: 'Wap', app_name: 'Vendure' },
                },
            })) as any;
            return { payType: 'h5', payUrl: r?.data?.h5_url };
        }
        if (type === 'APP') {
            const r = (await pay.transactions_app(baseParams)) as any;
            return { payType: 'app', prepayId: r?.data?.prepay_id };
        }
        // JSAPI
        const r = (await pay.transactions_jsapi({
            ...baseParams,
            payer: { openid: input.openid || '' },
        })) as any;
        const prepayId = r?.data?.prepay_id;
        const timeStamp = String(Math.floor(Date.now() / 1000));
        const nonceStr = Math.random().toString(36).substring(2, 34);
        const pkg = `prepay_id=${prepayId}`;
        const paySign = crypto
            .sign('RSA-SHA256', Buffer.from(`${appId}\n${timeStamp}\n${nonceStr}\n${pkg}\n`), {
                key: Buffer.from(privateKey),
            })
            .toString('base64');
        return { payType: 'jsapi', prepayId, appId, timeStamp, nonceStr, package: pkg, signType: 'RSA', paySign };
    }
}
```

- [ ] **Step 3: 导出**

修改 `packages/wechatpay-plugin/index.ts`：

```ts
export * from './src/plugin';
export * from './src/types';
export * from './src/constants';
export * from './src/wechatpay.service';
export * from './src/wechatpay-settlement';
```

- [ ] **Step 4: 提交**

```bash
git add packages/wechatpay-plugin/src/wechatpay-settlement.ts packages/wechatpay-plugin/src/wechatpay.service.ts packages/wechatpay-plugin/index.ts
git commit -m "feat(wechatpay): 网关服务 WechatpayService.createBarePayment + 结算注册表"
```

---

## Task 2: wechatpay-plugin 通用建支付 mutation + import 安全化

**Files:**
- Create: `packages/wechatpay-plugin/src/wechatpay-shop.resolver.ts`
- Modify: `packages/wechatpay-plugin/src/plugin.ts`

- [ ] **Step 1: 建 shop resolver**

创建 `packages/wechatpay-plugin/src/wechatpay-shop.resolver.ts`：

```ts
import { Args, Mutation, Resolver } from '@nestjs/graphql';
import { Allow, Permission } from '@vendure/core';

import { WechatpayService, BarePaymentInput } from './wechatpay.service';

@Resolver()
export class WechatpayShopResolver {
    constructor(private wechatpayService: WechatpayService) {}

    @Mutation()
    @Allow(Permission.Authenticated)
    wechatpayCreatePayment(
        @Args('input') input: BarePaymentInput,
    ): Promise<unknown> {
        return this.wechatpayService.createBarePayment(input);
    }
}
```

- [ ] **Step 2: 改 plugin.ts（providers/schema/守卫）**

在 `packages/wechatpay-plugin/src/plugin.ts` 中：
1. import 增：`WechatpayService`、`WechatpaySettlementRegistry`、`WechatpayShopResolver`、`Logger`（已有 Logger）。
2. `providers` 增：`WechatpayService`、`WechatpaySettlementRegistry`。
3. 增 `shopApiExtensions`。
4. `onApplicationBootstrap` 增守卫 `if (!this.options?.devBypass) return;`（原为 `if (!this.options.devBypass)`）。

最终关键片段：

```ts
import { Inject, OnApplicationBootstrap, Type } from '@nestjs/common';
import { LanguageCode, Logger, PaymentMethodService, ChannelService, PluginCommonModule, RequestContext, VendurePlugin } from '@vendure/core';
import { WECHATPAY_PLUGIN_OPTIONS, loggerCtx } from './constants';
import { createWechatpayHandler } from './wechatpay-handler';
import { WechatpayController } from './wechatpay.controller';
import { WechatpayPluginOptions } from './types';
import { WechatpayService } from './wechatpay.service';
import { WechatpaySettlementRegistry } from './wechatpay-settlement';
import { WechatpayShopResolver } from './wechatpay-shop.resolver';

const { gql } = require('graphql-tag');

@VendurePlugin({
    imports: [PluginCommonModule],
    controllers: [WechatpayController],
    providers: [
        { provide: WECHATPAY_PLUGIN_OPTIONS, useFactory: () => WechatpayPlugin.options },
        WechatpayService,
        WechatpaySettlementRegistry,
    ],
    shopApiExtensions: {
        schema: () => gql`
            type WechatpayCreatePaymentResult {
                payType: String!
                prepayId: String
                appId: String
                timeStamp: String
                nonceStr: String
                package: String
                signType: String
                paySign: String
                payUrl: String
            }
            input WechatpayPaymentInput {
                outTradeNo: String!
                amount: Int!
                tradeType: String
                openid: String
                description: String
            }
            extend type Mutation {
                wechatpayCreatePayment(input: WechatpayPaymentInput!): WechatpayCreatePaymentResult!
            }
        `,
        resolvers: [WechatpayShopResolver],
    },
    configuration: config => {
        const handler = createWechatpayHandler(WechatpayPlugin.options);
        config.paymentOptions.paymentMethodHandlers = [
            ...(config.paymentOptions.paymentMethodHandlers || []),
            handler,
        ];
        return config;
    },
    compatibility: '^3.0.0',
})
export class WechatpayPlugin implements OnApplicationBootstrap {
    private static options: WechatpayPluginOptions = {};

    constructor(
        @Inject(WECHATPAY_PLUGIN_OPTIONS) private options: WechatpayPluginOptions,
        private paymentMethodService: PaymentMethodService,
        private channelService: ChannelService,
        private requestContextService: RequestContextService,
    ) {}

    static init(options: WechatpayPluginOptions): Type<WechatpayPlugin> {
        WechatpayPlugin.options = options;
        return WechatpayPlugin;
    }

    async onApplicationBootstrap() {
        if (!this.options?.devBypass) return;
        // ...原有自动创建 wechatpay PaymentMethod 逻辑保持不变...
    }
}
```

> 说明：保留 `static init` 签名不变（外部所有引用 `WechatpayPlugin.init(...)` 无需改）。`WechatpayPlugin.options = {}` 原生默认使插件可被无条件 import（import 安全化）。`shopApiExtensions` 需 `@VendurePlugin` 支持（本仓库版本支持）。

- [ ] **Step 3: 提交**

```bash
git add packages/wechatpay-plugin/src/wechatpay-shop.resolver.ts packages/wechatpay-plugin/src/plugin.ts
git commit -m "feat(wechatpay): 通用建支付 mutation wechatpayCreatePayment + 插件 import 安全化"
```

---

## Task 3: 网关回调按 out_trade_no 路由结算

**Files:**
- Modify: `packages/wechatpay-plugin/src/wechatpay.controller.ts`

- [ ] **Step 1: 注入 registry 并改结算路由**

修改 `wechatpay.controller.ts`：
1. import 增 `WechatpaySettlementRegistry`。
2. 构造函数增注入 registry。
3. 新增私有 `routeSettlement(outTradeNo, transactionId)`：`registry.find(outTradeNo)` 命中则建 admin ctx 调 `settle`；否则回退 `settleOrderPayment(outTradeNo)`。
4. `notify` 中把 `await this.settleOrderPayment(outTradeNo)` 换为 `await this.routeSettlement(outTradeNo, transactionId)`。
5. `dev-pay` 页面参数 `orderCode` → `outTradeNo`，`dev-notify` POST 传 `outTradeNo`。
6. `devNotify` 里 `settleOrderPayment(orderCode)` → `routeSettlement(outTradeNo, '')`。

关键改动片段：

```ts
import { WechatpaySettlementRegistry } from './wechatpay-settlement';
// 构造注入：
// constructor(
//   @Inject(WECHATPAY_PLUGIN_OPTIONS) private options: WechatpayPluginOptions,
//   private orderService: OrderService,
//   private channelService: ChannelService,
//   private paymentMethodService: PaymentMethodService,
//   private requestContextService: RequestContextService,
//   private settlementRegistry: WechatpaySettlementRegistry,
// ) {}

/** 结算路由：非订单前缀（如 RC-）交给注册的结算器，否则默认结 Vendure Order */
private async routeSettlement(outTradeNo: string, transactionId: string): Promise<void> {
    const settlement = this.settlementRegistry.find(outTradeNo);
    if (settlement) {
        const channel = await this.channelService.getDefaultChannel();
        const ctx = new RequestContext({
            apiType: 'admin',
            channel,
            isAuthorized: true,
            authorizedAsOwnerOnly: false,
        });
        await settlement.settle(ctx, outTradeNo, transactionId);
        return;
    }
    await this.settleOrderPayment(outTradeNo);
}
```

`notify` 内（`TRANSACTION.SUCCESS` 分支）改：

```ts
if (body?.event_type === 'TRANSACTION.SUCCESS' && outTradeNo) {
    await this.routeSettlement(outTradeNo, transactionId || '');
}
```

`dev-pay` 页面把 `orderCode` 全部替换为 `outTradeNo`（脚本与标签）。

`devNotify` 改：

```ts
@Post('dev-notify')
async devNotify(@Req() req: Request, @Res() res: Response) {
    const outTradeNo = (req.query as any).outTradeNo as string;
    try {
        if (!outTradeNo) {
            return res.status(400).json({ code: 'FAIL', message: 'missing outTradeNo' });
        }
        await this.routeSettlement(outTradeNo, 'DEV-TX');
        res.status(200).json({ code: 'SUCCESS', message: 'OK' });
    } catch (e: any) {
        Logger.error(`WeChat Pay dev-notify error: ${e.message}`, loggerCtx);
        res.status(500).json({ code: 'FAIL', message: e.message });
    }
}
```

- [ ] **Step 2: 提交**

```bash
git add packages/wechatpay-plugin/src/wechatpay.controller.ts
git commit -m "feat(wechatpay): 回调按 out_trade_no 前缀路由结算（默认仍结 Order）"
```

---

## Task 4: recharge-card 在线充值支付与网关结算

**Files:**
- Modify: `packages/recharge-card-plugin/src/recharge-order.entity.ts`
- Modify: `packages/recharge-card-plugin/src/recharge-card.service.ts`
- Modify: `packages/recharge-card-plugin/src/recharge-order.resolver.ts`
- Modify: `packages/recharge-card-plugin/src/plugin.ts`

- [ ] **Step 1: RechargeOrder 增 externalRef 列**

修改 `packages/recharge-card-plugin/src/recharge-order.entity.ts` 增：

```ts
@Column({ type: 'varchar', nullable: true })
externalRef: string | null;
```

（构造签名无需改，`externalRef` 与 `remark` 同风格可空。）

- [ ] **Step 2: 服务层增两个方法**

在 `recharge-card.service.ts` 增 import：

```ts
import { WechatpayService } from '@vendure/wechatpay-plugin';
```

并将方法加在 `findMyRechargeOrders` 后：

```ts
/** 在线充值：生成微信支付参数并在充值单回写支付方式（仅本人 pending 单） */
async createWechatRechargePayment(
    ctx: RequestContext,
    rechargeOrderId: ID,
    tradeType?: string,
    openid?: string,
): Promise<any> {
    const cid = await this.resolveCustomerId(ctx);
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = await repo.findOne({
        where: { id: rechargeOrderId as any, customerId: cid, channelId: ctx.channelId as any },
    });
    if (!order) throw new UserInputError('Recharge order not found');
    if (order.status !== 'pending') {
        throw new UserInputError(`Recharge order is already ${order.status}`);
    }
    if (!this.wechatpayService) {
        throw new UserInputError('Payment gateway not configured');
    }
    const outTradeNo = `RC-${order.id}`;
    const pay = await this.wechatpayService.createBarePayment({
        outTradeNo,
        amount: order.amount,
        tradeType: (tradeType as any) || 'JSAPI',
        openid,
        description: `Recharge ${outTradeNo}`,
    });
    order.paymentMethod = 'wechatpay';
    order.externalRef = outTradeNo;
    await repo.save(order);
    return { rechargeOrderId: order.id, outTradeNo, pay };
}

/** 网关回调结算入口：解析 RC-<id>，原子入账（admin ctx 无 activeUser，用单归属 customerId 显式入账） */
async settleRechargeOrderByOutTradeNo(
    ctx: RequestContext,
    outTradeNo: string,
): Promise<void> {
    const m = String(outTradeNo).match(/^RC-(\d+)$/);
    if (!m) throw new UserInputError('Invalid recharge out_trade_no');
    const repo = this.connection.getRepository(ctx, RechargeOrder);
    const order = await repo.findOne({
        where: { id: m[1] as any, channelId: ctx.channelId as any },
    });
    if (!order) throw new UserInputError('Recharge order not found');
    if (order.status !== 'pending') return; // 幂等：已结算直接返回
    await this.connection.startTransaction(ctx);
    try {
        const claim = await repo.createQueryBuilder()
            .update(RechargeOrder)
            .set({ status: 'paid', paidAt: new Date() })
            .where('id = :id AND status = :status', { id: order.id, status: 'pending' })
            .execute();
        if (claim.affected === 0) {
            await this.connection.commitOpenTransaction(ctx);
            return; // 并发下已被他人结算，跳过
        }
        await this.applyBalanceChange(
            ctx,
            order.customerId,
            order.amount,
            BalanceTransactionType.RECHARGE,
            { remark: `Recharge order #${order.id}` },
        );
        await this.connection.commitOpenTransaction(ctx);
    } catch (e) {
        await this.connection.rollBackTransaction(ctx);
        throw e;
    }
}
```

**跨插件接线（关键：避免 import 整个 WechatpayPlugin 模块导致重复注册）**：recharge-card **不** import `WechatpayPlugin` 模块，而是利用 Vendure 应用级 `Injector.get(token, false)` 在 bootstrap 时**可选**拿到网关的 `WechatpayService` / `WechatpaySettlementRegistry`（两插件都注册在根 plugins 数组时，其 provider 都在全局容器，可被 injector 解析；未注册时为 undefined，recharge 保持独立可用）。

在服务层加网关 holder：

```ts
// recharge-card.service.ts 顶部
import { WechatpayService } from '@vendure/wechatpay-plugin';

let gatewayService: WechatpayService | null = null;
export function setWechatpayGateway(gw: WechatpayService | null): void {
    gatewayService = gw;
}
```

构造函数**不改**（不加 wechatpay 注入），`createWechatRechargePayment` 内直接用 `gatewayService`：

```ts
if (!gatewayService) {
    throw new UserInputError('Payment gateway not configured');
}
const pay = await gatewayService.createBarePayment({ ... });
```

对应地，`settleRechargeOrderByOutTradeNo` 也被模块级注册的 `RC-` 结算器调用（见 plugin Step 4），无需额外注入。

> 依赖：`recharge-card-plugin/package.json` 的 `dependencies` 增 `"@vendure/wechatpay-plugin": "^{workspace版本}"`（用于类型与 token 常量编译，此为单向软依赖，不影响运行时可选项）。若仓库用 workspace 协议，改为对应版本号。

- [ ] **Step 3: resolver 增 mutation**

在 `recharge-order.resolver.ts` 增 mutation：

```ts
@Mutation()
@Allow(Permission.Authenticated)
async createWechatRechargePayment(
    @Ctx() ctx: RequestContext,
    @Args('rechargeOrderId') rechargeOrderId: number,
    @Args('tradeType', { nullable: true }) tradeType?: string,
    @Args('openid', { nullable: true }) openid?: string,
): Promise<any> {
    return this.rechargeCardService.createWechatRechargePayment(ctx, rechargeOrderId, tradeType, openid);
}
```

- [ ] **Step 4: plugin.ts 增 import/结算注册/schema**

在 `recharge-card-plugin/src/plugin.ts`：
1. import 增：
   ```ts
   import { Inject, OnApplicationBootstrap, Type } from '@nestjs/common';
   import { Injector, Logger, OrderService, PluginCommonModule, VendurePlugin } from '@vendure/core';
   import { WechatpayService, WechatpaySettlementRegistry } from '@vendure/wechatpay-plugin';
   import { setWechatpayGateway } from './recharge-card.service';
   import { RechargeCardService } from './recharge-card.service';
   ```
2. `imports` **保持** `[PluginCommonModule]`（**不**加 WechatpayPlugin，避免重复注册）。
3. `onApplicationBootstrap(injector: Injector)` 改为接收 injector，末尾增：
   ```ts
   async onApplicationBootstrap(injector: Injector): Promise<void> {
       this.setBalanceHandlerSeed();
       // 可选接入网关：应用内注册了 WechatpayPlugin 时解析到，否则 null（独立可用）
       const registry = injector.get(WechatpaySettlementRegistry, false);
       const gateway = injector.get(WechatpayService, false);
       setWechatpayGateway(gateway ?? null);
       if (registry) {
           const svc = this.rechargeCardService;
           registry.register({
               prefix: 'RC-',
               settle: (ctx, outTradeNo) => svc.settleRechargeOrderByOutTradeNo(ctx, outTradeNo),
           });
           Logger.info('RechargeOrder ~RC- settlement registered (wechatpay gateway)', loggerCtx);
       }
   }
   ```
   > `this.setBalanceHandlerSeed()` 替换原有 `setRechargeService(...)`/`setOrderService(...)` 两行到私有方法里，或直接保留原两行不变，把网关接线追加在其后。

4. shop `mutation` 增：
   ```graphql
   createWechatRechargePayment(rechargeOrderId: ID!, tradeType: String, openid: String): WechatRechargePaymentResult!
   ```
5. shop schema `type` 增（与 RechargeOrderItem 并列）：
   ```graphql
   type WechatRechargePaymentResult {
       rechargeOrderId: ID!
       outTradeNo: String!
       pay: WechatPayParams!
   }
   type WechatPayParams {
       payType: String!
       prepayId: String
       appId: String
       timeStamp: String
       nonceStr: String
       package: String
       signType: String
       paySign: String
       payUrl: String
   }
   ```

> 依赖/接线总览：根 `plugins` 数组同时注册 `RechargeCardPlugin.init()` 与 `WechatpayPlugin.init({...})`；recharge 不 import 网关模块，bootstrap 用 `Injector.get(...,false)` 拿到网关注册 `RC-` 结算与支付服务（两 provider 均在应用容器，可解析）。现有不用 wechatpay 的场景下 `injector.get(...,false)` 返回 undefined，充值支付提示网关未配置，卡密/余额功能不受影响。

- [ ] **Step 5: 提交**

```bash
git add packages/recharge-card-plugin/src/recharge-order.entity.ts packages/recharge-card-plugin/src/recharge-card.service.ts packages/recharge-card-plugin/src/recharge-order.resolver.ts packages/recharge-card-plugin/src/plugin.ts
git commit -m "feat(recharge-card): 在线充值微信支付 - createWechatRechargePayment + RC- 网关结算入账"
```

---

## Task 5: wechatpay 网关 e2e（建支付 + 结算路由 + 默认结单回归）

**Files:**
- Create: `packages/wechatpay-plugin/e2e/wechatpay.e2e-spec.ts`

- [ ] **Step 1: 建 e2e**

创建 `packages/wechatpay-plugin/e2e/wechatpay.e2e-spec.ts`：

```ts
import { createTestEnvironment, registerInitializer, SqljsInitializer } from '@vendure/testing';
import { afterAll, beforeAll, describe, expect, it } from 'vitest';
import path from 'path';
import gql from 'graphql-tag';
import { mergeConfig } from '@vendure/core';
import { initialData } from '../../e2e-common/e2e-initial-data';
import { TEST_SETUP_TIMEOUT_MS, testConfig } from '../../e2e-common/test-config';
import { WechatpayPlugin } from '../src/plugin';

registerInitializer('sqljs', new SqljsInitializer(path.join(__dirname, '__data__')));

describe('WechatpayPlugin · 通用支付网关', () => {
    const { server, adminClient, shopClient } = createTestEnvironment(
        mergeConfig(testConfig(), {
            plugins: [WechatpayPlugin.init({ notifyUrl: 'http://localhost/wechatpay/notify', devBypass: true })],
        }),
    );

    async function createCustomerAndLogin(email: string): Promise<string> {
        const res = (await adminClient.query(gql`
            mutation {
                createCustomer(input: { firstName: "G", lastName: "W", emailAddress: "${email}" }, password: "test") {
                    ... on Customer { id emailAddress }
                }
            }
        `)) as any;
        await shopClient.asUserWithCredentials(email, 'test');
        return res.createCustomer.id;
    }

    beforeAll(async () => {
        await server.init({
            initialData: { ...initialData },
            productsCsvPath: path.join(__dirname, '../../core/e2e/fixtures/e2e-products-minimal.csv'),
        });
        await adminClient.asSuperAdmin();
        await createCustomerAndLogin('gateway.pay@test.com');
    }, TEST_SETUP_TIMEOUT_MS);

    afterAll(async () => {
        await server.destroy();
    });

    it('wechatpayCreatePayment（devBypass）返回模拟支付 URL', async () => {
        const r = await shopClient.query(gql`
            mutation {
                wechatpayCreatePayment(input: { outTradeNo: "T-1", amount: 10000, tradeType: "JSAPI", description: "t" }) {
                    payType payUrl
                }
            }
        `);
        expect(r.wechatpayCreatePayment.payType).toBe('dev-h5');
        expect(r.wechatpayCreatePayment.payUrl).toContain('outTradeNo=T-1');
    });

    it('wechatpayCreatePayment NATIVE/APP 返回对应 payType（devBypass 统一 dev-h5）', async () => {
        for (const t of ['NATIVE', 'APP', 'H5']) {
            const r = await shopClient.query(gql`
                mutation { wechatpayCreatePayment(input: { outTradeNo: "T-2", amount: 200, tradeType: "${t}" }) { payType payUrl } }
            `);
            expect(r.wechatpayCreatePayment.payType).toBe('dev-h5');
        }
    });
});
```

- [ ] **Step 3: 运行验证 + 提交**

```powershell
$env:PACKAGE='wechatpay-plugin'; npx vitest run --config packages/wechatpay-plugin/vitest.config.mts e2e/wechatpay.e2e-spec.ts
```
预期全绿。改实体 schema 变化时删 `e2e/__data__/*.sqlite`。
```bash
git add packages/wechatpay-plugin/e2e/wechatpay.e2e-spec.ts
git commit -m "test(wechatpay): 通用建支付 e2e"
```

> 若 `packages/wechatpay-plugin/vitest.config.mts` 不存在，参考 recharge-card-plugin 的先补一个（或复用根 scripts）。实现时按仓库实测为准。

---

## Task 6: recharge-card 在线充值支付 e2e（全流程）

**Files:**
- Modify: `packages/recharge-card-plugin/e2e/recharge-card.e2e-spec.ts`

- [ ] **Step 1: plugins 增我们chatpay + import**

在文件顶部把 `mergeConfig` 的 `plugins` 改为同时注册两个插件：

```ts
import { WechatpayPlugin } from '@vendure/wechatpay-plugin';

const { server, adminClient, shopClient } = createTestEnvironment(
    mergeConfig(testConfig(), {
        plugins: [
            RechargeCardPlugin.init(),
            WechatpayPlugin.init({ notifyUrl: 'http://localhost/wechatpay/notify', devBypass: true }),
        ],
    }),
);
```

- [ ] **Step 2: 新增在线充值用例（确定性结算触发）**

文件顶部 import 增（用真实 import，勿用 require）：

```ts
import { ChannelService, RequestContext } from '@vendure/core';
import { WechatpayPlugin, WechatpaySettlementRegistry } from '@vendure/wechatpay-plugin';
```
> 注意：`WechatpayPlugin` 需在顶部已 import（Step 1 已加 `import { WechatpayPlugin } from '@vendure/wechatpay-plugin';`）。

describe 内加结算辅助（等价网关 notify 的 admin ctx 路由触发）：

```ts
async function settleViaGateway(outTradeNo: string, txId = 'DEV-TX'): Promise<void> {
    const registry = server.app.get(WechatpaySettlementRegistry);
    const settlement = registry.find(outTradeNo);
    expect(settlement).toBeDefined();
    const channel = (await server.app.get(ChannelService).getDefaultChannel());
    const ctx = new RequestContext({ apiType: 'admin', channel, isAuthorized: true, authorizedAsOwnerOnly: false });
    await settlement!.settle(ctx, outTradeNo, txId);
}
```

用例改为：

```ts
it('在线充值：建单→建支付参数→网关结算→余额到账+流水', async () => {
    const created = (await shopClient.query(gql`
        mutation { createRechargeOrder(amount: 5000, remark: "wc") { id status } }
    `)) as any;
    const orderId = created.createRechargeOrder.id;

    const pay = (await shopClient.query(gql`
        mutation { createWechatRechargePayment(rechargeOrderId: "${orderId}", tradeType: "JSAPI") { rechargeOrderId outTradeNo pay { payType payUrl } } }
    `)) as any;
    const outTradeNo = pay.createWechatRechargePayment.outTradeNo;
    expect(outTradeNo).toBe(`RC-${orderId}`);
    expect(pay.createWechatRechargePayment.pay.payType).toBe('dev-h5');

    // 触发 RC- 结算（等价网关 notify 路由）
    await settleViaGateway(outTradeNo, 'DEV-TX');

    const after = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
    const list = (await shopClient.query(gql`query { myRechargeOrders { id status paymentMethod } }`)).myRechargeOrders;
    expect(after).toBe(5000);
    expect(list.find(o => o.id === orderId).status).toBe('paid');
});

it('在线充值：重复结算幂等（余额不再加）', async () => {
    const created = (await shopClient.query(gql`
        mutation { createRechargeOrder(amount: 2000) { id } }
    `)) as any;
    const orderId = created.createRechargeOrder.id;
    const pay = (await shopClient.query(gql`
        mutation { createWechatRechargePayment(rechargeOrderId: "${orderId}") { outTradeNo } }
    `)) as any;
    const outTradeNo = pay.createWechatRechargePayment.outTradeNo;
    await settleViaGateway(outTradeNo, 'DEV-TX1');
    await settleViaGateway(outTradeNo, 'DEV-TX2'); // 第二次应跳过

    const b = (await shopClient.query(gql`query { myRechargeBalance }`)).myRechargeBalance;
    expect(b).toBe(7000); // 5000 + 2000（仅一次入账）
});

it('在线充值：他人越权建支付被拒 / 已支付单建支付被拒', async () => {
    // 主顾客建单 + paid 后再次建支付
    const created = (await shopClient.query(gql`
        mutation { createRechargeOrder(amount: 1000) { id } }
    `)) as any;
    const orderId = created.createRechargeOrder.id;
    const pay = (await shopClient.query(gql`
        mutation { createWechatRechargePayment(rechargeOrderId: "${orderId}") { outTradeNo } }
    `)) as any;
    await settleViaGateway(pay.createWechatRechargePayment.outTradeNo, 'DEV-TX');

    // 已结算单再次建支付 → 报错
    await expect(shopClient.query(gql`
        mutation { createWechatRechargePayment(rechargeOrderId: "${orderId}") { outTradeNo } }
    `)).rejects.toThrow();
});
```

- [ ] **Step 3: 运行验证 + 提交**

```powershell
$env:PACKAGE='recharge-card-plugin'; npx vitest run --config packages/recharge-card-plugin/vitest.config.mts packages/recharge-card-plugin/e2e/recharge-card.e2e-spec.ts
```
预期 11 旧例 + 新增 3 例全绿；改实体 schema 时删 `e2e/__data__/*.sqlite`。
```bash
git add packages/recharge-card-plugin/e2e/recharge-card.e2e-spec.ts
git commit -m "test(recharge-card): 在线充值微信支付 e2e - 结算入账/幂等/越权"
```

---

## Task 7: 构建 + 文档 + 提交推送（部署铁律）

**Files:**
- Modify: `packages/recharge-card-plugin/README.md`
- Modify: `packages/wechatpay-plugin/README.md`（若有）

- [ ] **Step 1: 本地重建 lib**

```powershell
npx tsc -p packages/recharge-card-plugin/tsconfig.build.json
npx tsc -p packages/wechatpay-plugin/tsconfig.build.json
```
> 若 tsc 编译通过则入库产物；若有 TypeScript 报错按报错修正后重跑（尤其跨包 DI 的 `@Optional()` 与 `@vendure/wechatpay-plugin` 依赖解析）。

- [ ] **Step 2: 更新 README（能力/接入点/测试命令）**

在 recharge-card README 增「在线充值（微信支付）」：
- `createRechargeOrder(amount)` → `createWechatRechargePayment(rechargeOrderId, tradeType?, openid?)` 拿支付参数；网关回调命中 `RC-` 自动入账。
- 幂等：重复入账被 `UPDATE ... WHERE status='pending'` 拦截。
- 前置：需注册 `WechatpayPlugin` 且充值单归属与支付人一致。

- [ ] **Step 3: 提交并推送**

```bash
git add packages/recharge-card-plugin/lib packages/wechatpay-plugin/lib packages/recharge-card-plugin/README.md
git commit -m "build+docs(phase34): lib 本地重建(部署铁律) + 在线充值文档"
git push origin master
```

---

## 验收清单（spec 覆盖）

- [ ] 通用网关：`wechatpayCreatePayment` 返回 jsapi/h5/native/app 或 dev 支付参数
- [ ] 回调路由：`RC-` 前缀→充值结算；其它→默认 Order 结算（向后兼容）
- [ ] 在线充值：`createWechatRechargePayment` 校验本人 pending 单 + 回写 paymentMethod/externalRef
- [ ] 入账：结算后 RechargeOrder=paid + 余额增加 + `RECHARGE` 流水
- [ ] 幂等：重复回调/重复结算余额不变
- [ ] 越权：他人/已结算单建支付被拒
- [ ] 现有 11 例 recharge e2e 继续全绿